# TW-Overlay v3.0.0 정식 릴리즈 구현 계획

작성일: 2026-08-24  
상태: **사용자 승인 완료 — Phase 1·2 완료, Phase 3 분리 파일·다중 PC 자동 수렴·부분 복원·종료 복구 핵심 경로 구현 및 강화 검증 진행 중, Phase 5 동적 Z-order·독 재사용 실기 검증 완료**

## 0. 최우선 개발·릴리즈 원칙

이번 작업의 최우선 목표는 **현재 제공 중인 모든 기능의 정상 동작을 보존하면서 실제 오류를 해결하는 것**이다. 계획 항목 수나 구조 개선 달성률보다 기존 사용자 흐름의 무회귀가 우선한다.

- 구조 개선은 결함 해결에 필요한 범위로 제한하고 기존 공개 동작·저장 형식·UI 흐름과의 호환성을 먼저 검토한다.
- 변경은 기능별 소규모 커밋으로 분리하며 각 커밋 전 `npm run typecheck`, 전체 `npm test`, `git diff --check`를 통과시킨다.
- 기존 동작을 검증하는 테스트가 없으면 수정 전에 재현 또는 회귀 테스트를 추가한다.
- 개발 의도가 확정되지 않은 동작은 임의로 바꾸지 않고, 확정된 사용자 답변과 실제 코드 계약을 기준으로 처리한다.
- 테스트가 실패한 변경은 원인 해결과 전체 재검증 전까지 다음 작업이나 완료 상태로 넘기지 않는다.
- Phase 완료 판정은 개별 계획 항목뿐 아니라 앱 시작, 숙제 자동 감지·수동 조작, 일지, 알림, 오버레이, 종료·재시작 등 실제 사용자 흐름의 전체 회귀 검증을 포함한다.

현재 Phase 2에서 완료하고 전체 자동 검증을 통과한 단위:

- 기본 사냥터 지도 3개 복구와 멱등 생성
- 수동 일지 row ID 단건 삭제 및 자동 기록 보호
- 숙제 재완료 시 최신 시각·메타데이터 갱신
- 숙제 pending 이벤트 순서와 리셋 경계 보존
- 레거시 숙제 ID 선행 정규화와 현재 리셋 주기 기준 상태 병합, 채팅 source event ID 중복 방지
- 채팅 주간 동기화 배치 rollback 실패 전파
- 경험의 정수 100억 단위 판정의 XP HUD·일지 일원화
- `1조/억/만` 레거시 금액 복구와 화면 복합 금액 파싱
- 어벤던로드 입장료의 즉시 차감·15초 지역 귀속·미귀속 보존
- 레거시 숙제 가시성 기본값 보존
- 숙제 데이터 정상 시작 경로 1회 초기화
- v1/v2/v3 SQLite 버전 마이그레이션의 트랜잭션 안전성
- 절전 중 놓친 커스텀·필드보스 알림의 무음 이력 기록과 복귀 타이머 중복 방지
- 엘소 종료 flush 실패의 recovery journal·operation ID 기반 정확히 한 번 재생
- 숙제 로그·통계와 알림/외치기 핵심 DB 쓰기의 명시적 실패 반환, 최신 숙제 상태만 제한 재시도, 종료 시 재시도 취소
- 수량의 자릿수 쉼표(`1,000개`)를 복수 아이템 구분자로 오인하지 않는 파싱
- 외치기 레거시 중복 정리의 실제 5초 시간창 보장
- DB 초기화 실패 후 `getStmt()`가 무효 연결로 계속하지 않고 명시적 오류를 반환하는 경계

현재 Phase 5에서 우선 완료하고 자동 검증을 통과한 작업표시줄·포커스 단위:

- 독·채팅 오버레이·자동 재생성 창은 `showInactive()`로 표시한다. 독은 hover뿐 아니라 클릭 IPC도 받아야 하므로 `focusable:false`로 만들지 않고, 실제 사용자 클릭 시에만 정상 TW-Overlay 전경 창이 된다.
- 사용자가 명시적으로 연 입력 도구 창의 기존 `show()`/포커스 동작은 보존
- 순간적 게임 미탐지·최소화 판정을 2회 연속 표본으로 재확인해 불필요한 `hideAll()`·창 재생성 방지
- 외부 프로그램이 전경이면 지연 포커스 복구를 차단하고, 게임·TW-Overlay 묶음을 즉시 Non-Topmost로 내려 `외부 프로그램 > TW-Overlay > 테일즈위버` 순서로 배치
- 일반 창모드·창모드 전체화면 모두에서 게임 Z-order를 직접 옮기는 `placeGameBelowWindow()` API 제거, 실제 `IsIconic`일 때만 `SW_RESTORE` 호출, 자동 `BringWindowToTop`·가상 Alt 키 제거
- `v2.6.0→v2.6.1` 이력에서 게임의 우발적 Topmost 승격 방지가 도입된 회귀 경계를 확인했다. 과거의 고착 버그는 복원하지 않고, 게임 또는 TW-Overlay가 foreground인 동안만 두 창 묶음을 동적으로 Topmost 계층에 올려 `TW-Overlay > 테일즈위버 > 작업표시줄`을 보장한다.
- 외부 앱 foreground, 게임 최소화·종료, TW-Overlay 종료에서는 게임을 반드시 `HWND_NOTOPMOST`로 되돌린다. 상태 변화가 없는 플레이 중에도 작업표시줄이 묶음 위로 올라왔는지만 읽기 검사하고 필요한 경우에만 재배치한다.
- 듀얼 모니터에서는 외부 foreground 창의 실제 bounds가 게임 전체화면과 겹칠 때만 묶음을 강등한다. 반대편 모니터에만 있는 외부 앱 포커스는 게임 모니터의 묶음을 유지하며, 창이 두 화면에 걸치거나 게임 화면으로 이동하면 즉시 강등한다.
- 위 계약을 고정하는 회귀 검사와 전체 `npm test` 통과. 단, 실제 Windows 듀얼 모니터·장시간 플레이 증상이 완전히 사라졌다는 판정은 실기 검증 후에만 내린다.

현재 Phase 3에서 완료하고 자동 검증을 통과한 클라우드 단위:

- `tw_overlay_settings.json`, `tw_overlay_checklist.json`, `tw_overlay_sync_meta.json`의 고정 이름과 이름별 Drive 검색·범용 JSON 업/다운로드 경계 추가
- 일반 설정과 숙제·캐릭터·pending 이력의 allowlist·payload builder 분리
- Discord Webhook URL, 절대경로·커스텀 사운드 ID의 업로드 제외와 비정상 원격 payload가 로컬 비밀값·사운드를 덮지 못하는 병합 방어
- `pendingHomeworks`는 숙제 전용 builder에 포함한다. 클라우드 기능은 미배포 개발 단계이므로 기존 단일 파일 형식의 호환·이전 계약은 두지 않는다.
- 설정 1.5초·숙제 0.5초의 독립 dirty/debounce와 single-flight Drive 전송 큐를 실제 세 파일 업로드에 연결했다. 개발 중 단일 파일은 조회·병합·마이그레이션하지 않는다.
- PC별 `cloud-sync-state.json`에 installation ID·최초 프로필 상태, 공유 generation, 파일 ID, 원격 revision, 설정 dirty key, 숙제 outbox/확인 operation과 base snapshot을 원자 저장한다.
- 숙제는 마지막 정상 동기화본 기준 `base/local/remote` 3방향 병합을 적용하고, 원격만 변경된 완료·삭제는 수신하며 같은 필드 양쪽 충돌만 실제 플레이 로컬을 우선한다.
- 회사·집 상시 실행 시 게임 실행 중 약 30초, 유휴 시 약 5분 pull을 수행하고 앱 시작·게임 시작·절전 복귀·화면 잠금 해제·자동 동기화 재활성화에는 즉시 pull한다.
- 메타 파일의 Drive file ID·공유 generation을 이름 검색보다 우선하고, 데이터 payload의 종류·schema·revision·checksum·크기·allowlist를 적용 전에 검증한다.
- 로그아웃·인증 무효화는 진행 Drive 요청을 취소하며 Drive 401은 access token 강제 갱신 후 한 번만 재시도한다.
- 숙제 업로드 직후 원격 revision·operation ID를 재조회해 확인된 operation만 outbox에서 제거하고, 이후 pull에서 확인 operation이 원격에서 사라졌으면 outbox에 다시 넣어 재게시한다.
- 숙제 operation은 값 없는 ID가 아니라 안정 ID 경로의 `before/after` mutation을 함께 보존한다. 확인 직후 다른 PC가 payload를 덮어써도 누락 operation의 변경만 최신 원격 상태 위에 재실행해 비충돌 변경을 함께 살린다.
- 메모리 Drive 통합 검사에서 설정/숙제/메타 분리 업로드와 `회사 숙제 변경 → 집 pull → 로컬 UI 상태 반영 → echo upload 없음` 흐름을 검증한다.
- 메모리 Drive 교차 업로드 검사에서 서로 다른 숙제 상태 변경, 같은 숙제의 완료/해제/횟수 충돌, 업로드 확인 직후 overwrite, 응답 유실, 로컬 상태 파일 재로드 후 재수렴을 검증했다. 독립 userData의 실제 `main.js` 두 프로세스에서도 정상 기준 상태에서 회사 PC의 완료·횟수 1과 집 PC의 같은 캐릭터 횟수 2·시각 변경을 교차시켜, 전체 operation 로그의 결정적 재생 결과와 두 operation ID가 원격·양쪽 로컬에 수렴하고 재시작 뒤 echo upload가 없음을 확인했다. 원격 적용 뒤 `contentsChecker.init()`에서 파생 echo outbox가 생기지 않도록 origin guard를 전체 적용 구간에 유지한다.
- pull·업로드에는 installation별 jitter와 429·오프라인을 포함한 지수 백오프를 적용하고, 네트워크 오프라인→온라인 전환은 10초 이내 감지해 즉시 pull한다. 실제 매니저 scheduler 실행 검사에서 게임 실행은 30초 기준 27~33초, 유휴는 5분 기준 270~330초의 installation jitter 범위로 각각 다음 pull 하나를 예약한다.
- 새 PC 부분 복원 UI와 종료 recovery marker까지 자동 검증했다. 남은 강화 작업은 실계정 2-PC 및 실제 Windows 종료·로그오프 실기 검증이다.

## 1. 감사 기준과 현재 상태

- 7개 전문 검수 역할이 요청된 핵심 파일을 읽기 전용으로 전수 검토했다.
- 현재 작업 트리에는 감사 시작 전부터 36개 소스 파일, 약 1,187줄 추가/564줄 삭제의 변경이 존재했다. 이 변경은 사용자 소유 작업으로 간주해 보존한다.
- 기준선 `npm run typecheck` 통과.
- 기준선 `npm test` 통과:
  - 전체 빌드 통과
  - 정적 회귀 검사 통과
  - 렌더러 동작 검사 39개 통과
- 현재 `package.json` 버전은 `2.7.0-beta.1`이다. v3 백업 코드는 이미 존재하지만 실제 버전 판정 없이 실행되는 상태다.
- 감사 완료 시점의 변경분은 기준선 커밋 `3f72b21`로 보존했다. 사용자 승인 후 Phase 1 구현을 시작했다.

## 2. 릴리즈 판정

현재 상태는 **v3.0.0 릴리즈 보류**가 필요하다.

가장 높은 위험은 다음과 같다.

1. 부분 손상된 `contents.json`이 유효 JSON이기만 하면 사용자 숙제 상태를 대량 삭제할 수 있다.
2. SQLite 마이그레이션 일부가 실패해도 완료 버전을 기록하고, 신규 DB 기본 사냥터가 생성되지 않는다.
3. `1조` 레거시 금액이 `1`로 변환될 수 있고, 배치 롤백 뒤 성공 건수를 반환한다.
4. 동기화 업로드와 복원을 같은 Promise로 취급해 변경 업로드가 유실될 수 있다.
5. 종료 플러시 재진입과 짧은 타임아웃 때문에 DB/클라우드 정리가 끝나기 전에 프로세스가 종료될 수 있다.
6. 모험일지에 저장형 DOM XSS와 비동기 날짜 역전으로 인한 오삭제 경로가 있다.
7. 비정상적으로 큰 당일 채팅 파일과 주간 동기화 결과를 메모리에 전량 보관해 OOM 위험이 있다. 정상적인 수 MB 규모의 당일 채팅은 메모리 검색 정책을 유지한다.
8. 절전 복귀 이벤트가 겹치면 분 정렬 타이머가 영구 중복될 수 있다.

## 3. 확정 결함 목록

각 항목의 본문은 발견 당시 결함과 영향 범위를 추적하기 위해 원문을 유지한다. 현재 구현·검증 상태는 항목명 대괄호의 상태 표기를 기준으로 판정한다.

### A. 설정·부트스트랩·종료

- **A-01 [P1, 코드 수정·자동 검증 완료]** 설정 디바운스 쓰기 실패 후 pending 상태를 폐기해 재시도와 종료 플러시가 불가능하다 (`config.ts:168-187`).
- **A-02 [P1, 코드 수정·자동 검증 완료]** `config.load()`의 얕은 복사로 중첩 설정이 캐시와 별칭을 공유한다 (`config.ts:38-39`).
- **A-03 [P2, 코드 수정·자동 검증 완료]** 딥 머지가 일부 최상위 맵에만 적용돼 하위 기본값 또는 기존 키가 유실될 수 있다 (`config.ts:123-131`, `168-200`).
- **A-04 [P1, 코드 수정·자동 검증 완료]** v3 백업이 실제 버전 확인 없이 실행되며 일부 파일만 복사돼도 완료 플래그를 기록한다 (`bootstrap.ts:31-65`).
- **A-05 [P1, 코드 수정·자동 검증 완료]** v3 DB 백업에서 WAL/SHM 또는 SQLite 일관 스냅샷이 누락된다 (`bootstrap.ts:48`).
- **A-06 [P1, 코드 수정·자동 검증 완료]** 레거시와 표준 경로가 충돌할 때 원본 비교·백업 없이 레거시 파일과 미등록 파일을 삭제할 수 있다 (`bootstrap.ts:70-128`).
- **A-07 [P1, 코드 수정·자동 검증 완료]** 종료 플러시 중 두 번째 `app.quit()`이 `preventDefault()` 없이 통과한다 (`main.ts:242-275`).
- **A-08 [P1, 코드 수정·자동 검증 완료]** 종료 경로가 3초/업데이트 0.5초 뒤 끝나지만 진행 중 요청의 명시적 취소·서버 반영 확인·recovery marker가 없어, 제한 시간에 걸린 업로드의 성공 여부와 다음 실행 재시도가 불명확하다 (`main.ts:262-275`).
- **A-09 [P2, 코드 수정·자동 검증 완료]** 명시적 WAL 체크포인트가 정의만 되고 종료 경로에서 호출되지 않는다 (`diaryDb.ts:123-130`).

### B. 체크리스트·상태·트래커

- **B-01 [P0, 코드 수정·자동 검증 완료]** 불완전하지만 비어 있지 않은 `contents.json`이 파괴적 필터를 통과해 사용자 상태를 삭제할 수 있다 (`contentsChecker.ts:18-40`, `351-413`).
- **B-02 [P1, 코드 수정·자동 검증 완료]** 일반 시작 경로에서 `contentsChecker.init()`이 호출되지 않는다 (`main.ts:182-213`).
- **B-03 [P1, 코드 수정·자동 검증 완료]** 고대 렐릭 daily 레거시 ID 정규화 순서 때문에 완료 상태가 통합 전에 삭제된다 (`contentsChecker.ts:71-164`, `351-385`).
- **B-04 [P1, 코드 수정·자동 검증 완료]** 두 레거시 상태의 완료 시각을 최신값이 아닌 고정 우선순위로 선택한다 (`contentsChecker.ts:99-109`).
- **B-05 [P1, 코드 수정·자동 검증 완료]** 로컬 체크 해제가 수정 시각/tombstone을 남기지 않아 클라우드의 과거 완료 상태가 되살아난다 (`contentsChecker.ts:619-621`, `syncDataHelper.ts:189-207`).
- **B-06 [P1, 코드 수정·자동 검증 완료]** 리셋 전 pending을 리셋 후 이벤트와 합쳐 이전 주기 횟수를 이월한다 (`contentsChecker.ts:1104-1118`).
- **B-07 [P1, 코드 수정·자동 검증 완료]** 절대값 pending 뒤 증분 이벤트가 오면 기존 캐릭터 횟수를 이중 가산한다 (`contentsChecker.ts:1107-1116`, `1164-1172`).
- **B-08 [P2, 코드 수정·자동 검증 완료]** 어벤던로드의 시간창 밖 수수료가 다음 지역에 잘못 귀속된다 (`abandonedTracker.ts:53-96`).
- **B-09 [P3, 코드 수정·자동 검증 완료]** `isVisible` 미설정 레거시 항목은 체크리스트·요약·일지 통계에서 모두 보임(`!== false`)으로 해석한다. 실제 렌더러 fixture에서 해당 행이 표시되고 첫 토글은 숨김으로 전환되는 경계를 확인했다.

### C. SQLite·일지·아이템

- **C-01 [P1, 코드 수정·자동 검증 완료]** 신규 DB에 사냥터 동선 시뮬레이션용 기본 지도 3개(시오칸하임 대장간·골고다의 협곡·공허의 영역)와 이미지 경로/좌표 변환 메타데이터를 생성하는 INSERT가 없다 (`diaryDb.ts:209-220`). 지도 삭제 UI는 없으므로 사용자 삭제 복원 문제가 아니라 실제 회귀 결함이다.
- **C-02 [P1, 코드 수정·자동 검증 완료]** 개별 마이그레이션 실패를 삼킨 뒤 `user_version=1`을 기록한다 (`diaryDb.ts:271-382`).
- **C-03 [P1, 코드 수정·자동 검증 완료]** DB 초기화 실패 후 연결과 statement cache를 정리하지 않아 같은 세션 재시도가 차단된다 (`diaryDb.ts:385-388`).
- **C-04 [P1, 코드 수정·자동 검증 완료]** 레거시 `1조` 금액을 fallback이 `1`로 덮어쓴다 (`diaryDb.ts:538-547`).
- **C-05 [P1, 코드 수정·자동 검증 완료]** 배치 트랜잭션 롤백 뒤 누적 카운터를 성공 결과로 반환한다 (`diaryDb.ts:1115-1239`).
- **C-06 [P1, 코드 수정·자동 검증 완료]** 활동 삭제가 동일 날짜/type/content의 모든 행을 삭제하면서 점수는 한 번만 차감하고, UI도 수동/자동 출처를 구분하지 않아 일부 자동 감지 `calc`·`loot` 기록에 삭제 버튼을 노출한다 (`diaryDb.ts:751-758`, `diary.html:773-775`, `1416-1419`).
- **C-07 [P1, 코드 수정·자동 검증 완료]** 종료 직전 엘소 flush 실패 후 복원된 메모리 버퍼를 DB close와 함께 잃는다 (`diaryDb.ts:97-105`, `563-568`).
- **C-08 [P1, 코드 수정·자동 검증 완료]** 숙제 통계, 외치기, 단어 알림 등 여러 공개 쓰기 API에 SQLite 예외 경계가 없다 (`diaryDb.ts:828-850`, `1032-1071`, `1270-1382`).
- **C-09 [P2, 코드 수정·자동 검증 완료]** 렌더러 복합 금액 파서가 `1조 2억 3만`을 단위 합산이 아닌 연속 곱셈으로 계산한다 (`renderer/diary/log-utils.ts:5-13`).
- **C-10 [P2, 코드 수정·자동 검증 완료]** 숙제 로그의 `(date, content_id)`가 UNIQUE가 아니고 재완료 시 최신 완료시각·이름·category·type 갱신과 UI 갱신이 누락된다 (`diaryDb.ts:263`, `779-795`). 콘텐츠 1회 초기화권 사용 후 같은 리셋 주기에 다시 완료해도 일지에는 별도 이력을 추가하지 않고 최신 완료 한 건만 표시하는 것이 확정 정책이다.
- **C-11 [P2, 코드 수정·자동 검증 완료]** hot path 다수가 `getStmt()`를 사용하지 않아 준비문 캐시 효과가 제한적이다.
- **C-12 [P2, 코드 수정·자동 검증 완료]** 복수 아이템 문자열을 모든 쉼표에서 나눠 `1,000개` 수량을 깨뜨린다 (`itemAcquisition.ts:221`).
- **C-13 [P2, 코드 수정·자동 검증 완료]** 외치기 레거시 중복 정리가 실제 ±5초가 아닌 5초 버킷을 사용한다 (`diaryDb.ts:1079-1084`).
- **C-14 [P3, 코드 수정·자동 검증 완료]** `getStmt()`는 `initDb()` 뒤 연결을 다시 확인하고 실패하면 명시적 오류를 던진다. 강제 마이그레이션 실패 fixture에서 null 연결을 참조하지 않고 오류를 전달하며 부분 변경과 `user_version` 상승이 함께 롤백되는지 확인했다.

### D. 클라우드·OAuth·IPC

- **D-01 [P1, 코드 수정·자동 검증 완료]** 업로드와 복원을 동일 진행 Promise로 합쳐 호출 결과 의미가 바뀌고 dirty 업로드가 사라진다 (`cloudSyncManager.ts:115-117`, `171-173`).
- **D-02 [P1, 코드 수정·자동 검증 완료]** 로그아웃/인증 무효화가 이미 진행 중인 Drive 요청을 취소하지 못한다.
- **D-03 [P1, 코드 수정·자동 검증 완료]** Drive API 401을 인증 무효화로 처리하지 않아 반복 실패와 UI 상태 불일치가 발생한다 (`googleDriveSync.ts:33-35` 등).
- **D-04 [P1, 코드 수정·자동 검증 완료]** OAuth 취소/만료 후 진행 중 콜백이 뒤늦게 토큰을 저장할 수 있다 (`googleAuth.ts:339-467`).
- **D-05 [P2, 코드 수정·자동 검증 완료]** 유효 access token만 있고 refresh token이 없으면 UI는 로그인으로 보지만 Drive는 거부한다 (`googleAuth.ts:189-231`).
- **D-06 [P2, 코드 수정·자동 검증 완료]** 클라우드 JSON의 schemaVersion·크기·필드 타입을 검증하지 않고 설정에 병합한다 (`googleDriveSync.ts:87-92`, `syncDataHelper.ts:238-264`).
- **D-07 [P1, 코드 수정·자동 검증 완료]** 다수 IPC 채널이 `NaN`, `Infinity`, 범위, enum, 배열 크기, 문자열 길이를 메인 경계에서 검증하지 않는다 (`ipcHandlers.ts` 다수).
- **D-08 [P1, 코드 수정·자동 검증 완료]** 프로덕션 preload가 제한 없는 테스트 주입 API를 노출한다 (`preload.ts:440-464`).
- **D-09 [P1, 코드 수정·자동 검증 완료]** 설정 스냅샷과 숙제 병합 상태를 고정 파일 `tw_overlay_sync.json` 하나에 함께 저장해, 한 영역의 변경이 무관한 영역까지 다시 업로드하고 서로 다른 충돌 정책·재시도·복원 단위를 분리할 수 없다 (`googleDriveSync.ts:8`, `97-163`, `syncDataHelper.ts:12-120`).
- **D-10 [P1, 코드 수정·자동 검증 완료]** 두 PC에서 앱을 계속 켜 두는 경우 로컬 변경 PC는 자동 업로드하지만 반대편 PC는 로그인 직후·수동 복원 외에 원격 변경을 다시 조회하지 않는다. 회사 PC에서 반영한 숙제가 집 PC에 자동 수렴하지 않고, 집 PC가 이후 오래된 상태를 기준으로 변경할 위험이 있다 (`cloudSyncManager.ts:41-64`, `108-164`, `164-279`, `344-366`).
- **D-11 [P1, 코드 수정·자동 검증 완료]** 완전히 빈 `fresh` 프로필의 기본 숙제 61개 초기화가 사용자 operation으로 outbox에 기록되어, 최초 Google 복원에서 원격 숙제보다 먼저 재생될 수 있다. `fresh` 상태에서는 config 변경 리스너가 숙제 outbox를 만들지 않으며, 원격 파일이 없을 때만 최초 로그인 흐름이 현재 전체 숙제를 명시적으로 업로드한다. `established` 프로필의 로그인 전 변경은 계속 outbox에 보존한다.

### E. 채팅·파일 I/O·모니터

- **E-01 [P2, 코드 수정·자동 검증 완료]** 정상적인 하루치 채팅은 수 MB 수준이라 `_todayLines` 메모리 검색을 유지할 수 있지만, 파일 크기 안전 상한이 없어 비정상 로그에서 전파일 읽기·이중 디코딩으로 메모리 급증이 가능하다 (`chatLogManager.ts:179-249`).
- **E-02 [P1, 코드 수정·자동 검증 완료]** 주간 워커도 파일·검출 결과를 전량 누적하고 한 번에 메인으로 복제한다 (`chatLogSyncWorker.ts:76-81`, `384-459`).
- **E-03 [P1, 코드 수정·자동 검증 완료]** Tail의 EBUSY/EPERM 오류 후 객체가 남아 실시간 감시 재연결이 영구 차단될 수 있다 (`chatLogManager.ts:216-218`, `729-736`).
- **E-04 [P1, 코드 수정·자동 검증 완료]** 워커가 파일 읽기 실패를 무시하고 전체 성공을 반환한다 (`chatLogSyncWorker.ts:380-460`).
- **E-05 [P2, 코드 수정·자동 검증 완료]** 저장 데이터에 공백 문자열 키워드가 남으면 다수 채팅과 의도치 않게 일치할 수 있으므로 소비 직전 trim/filter가 필요하다 (`chatLogProcessor.ts:586-647`). 키워드 배열 자체가 0개인 경우 지정 단어·외치기 알림이 꺼지는 현재 동작은 정상이다.
- **E-06 [P1, 코드 수정·자동 검증 완료]** Tail 오류 뒤 첫 재연결 시점에 로그 파일이 아직 없으면 예약 콜백이 즉시 종료되어 2/4/8/16초 후속 재시도가 끊긴다. 같은 경로가 유지되는 동안 파일 부재는 다음 지수 백오프를 예약하고, 사용자가 경로를 바꾼 경우에만 기존처럼 해당 재시도를 중단한다 (`chatLogManager.ts:275-290`).
- **E-07 [P2, 코드 수정·자동 검증 완료]** 일시 접근 불가능한 사용자 지정 로그 경로를 다른 자동 탐색 경로로 덮어쓴다 (`chatLogManager.ts:717-723`).
- **E-08 [P2, 코드 수정·자동 검증 완료]** Windows Known Folder를 사용하지 않아 리디렉션된 한글/기업 OneDrive 문서 경로를 놓친다 (`chatLogPathFinder.ts:33-38`).
- **E-09 [P3, 코드 수정·자동 검증 완료]** 채팅 로그 파일명의 연·월·일을 `Date` 왕복 비교해 실제 존재하는 날짜만 허용한다. 정상일·윤년과 비윤년 2월 29일, 13월, 32일 fixture를 고정했다.
- **E-10 [P1, 코드 수정·자동 검증 완료]** 주간 워커는 잠긴 파일을 재시도·부분 실패로 격리하지만, 워커를 시작하기 전 메인의 fingerprint·인코딩 사전 검사가 같은 파일을 동기식으로 열다가 `EBUSY`를 throw해 전체 동기화를 즉시 중단한다. 사전 검사에도 4회 제한 재시도를 적용하고 끝내 실패한 파일을 워커 실패 목록과 합쳐 정상 파일 결과를 보존한다 (`chatLogSyncManager.ts:197-236`, `chatLogSyncState.ts:201-218`).
- **E-11 [P2, 코드 수정·자동 검증 완료]** 대형 로그의 헤더 표본 끝과 최근 16MB 시작이 유효한 UTF-8 멀티바이트 중간에 걸리면, 이후 버리는 불완전 행의 대체 문자까지 손상 판정에 포함해 정상 파일에 “문자 손상” 경고를 표시한다. 실제 보존하는 헤더 25줄과 첫 불완전 행 제거 뒤 최근 구간에서만 손상을 판정한다 (`chatLogFileReader.ts:64-92`).

### F. 창·Win32·DPI

- **F-01 [P1, 코드 수정·자동 검증 완료/실기 대기]** display 리스너를 브라우저 오버레이 생성 경로에서 앱 초기화로 옮기고 한 번만 등록한다.
- **F-02 [P1, 코드 수정·자동 검증 완료/실기 대기]** `display-added`/`display-removed` 복구가 registry 창과 실제 `overlayWindow`를 함께 검사한다. 게임 좌표가 없을 때의 임시 중앙 복구는 programmatic move로 표시해 사용자 오프셋을 덮어쓰지 않는다.
- **F-03 [P2, 코드 수정·자동 검증 완료/실기 대기]** `display-metrics-changed`를 짧게 디바운스하고 같은 display 집합이 안정된 뒤 최신 물리 게임 rect를 다시 조회해 전체 창을 DIP 좌표로 재동기화한다.
- **F-04 [P2, 코드 수정·자동 검증 완료/실기 대기]** 일반 관리 창의 초기 폭·높이를 현재 모니터 작업 영역 안으로 제한하고, 사용자 크기 조절 창의 최소 크기도 사용 가능한 작업 영역보다 커지지 않게 조정한다. 사용자 저장 크기는 범위 안에서 우선하고 독·게임 오버레이의 게임 기준 고정 크기는 유지한다.
- **F-05 [P2, 코드 수정·자동 검증 완료]** 일반 보스·커스텀·사기 탐지 토스트의 닫기 버튼과 자동 만료를 공통 제거 경로로 통일하고, interactive 토스트 ID 참조 수가 0이 된 경우에만 사이드바 마우스 투과와 축소 폭을 복원한다.
- **F-06 [P3, 코드 수정·자동 검증 완료]** 프로세스 우선순위 상승을 게임 PID별 single-flight로 제한하고, 실패 시 `1초 → 2초 → 4초 → … → 최대 60초` 지수 백오프를 적용한다. 성공·게임 종료·PID 변경 시 상태를 초기화하며 기존 CloseHandle 경로는 유지한다.
- **F-07 [P1, 7차 이력·실기 기반 수정 완료/재검증 필요]** 독 이동·재표시 완료와 게임 foreground 복구가 모두 성공하고 DevTools를 분리한 뒤에도 증상이 동일했다. 릴리즈 이력 역추적 결과 `v2.6.1`에서 게임의 우발적 Topmost 전염을 막기 위해 `HWND_NOTOPMOST`와 `BringWindowToTop` 제거가 도입됐으며, 정상 동작하던 과거에는 이 부작용이 작업표시줄을 덮고 있었다. 게임/TW-Overlay foreground에서만 전체 묶음을 Topmost로 올리고 외부 앱 foreground·최소화·종료 시 즉시 Non-Topmost로 강등한다. 실기에서 작업표시줄 복구와 독 클릭은 정상화됐다. 추가로 시작 메뉴의 Topmost `SearchHost` HWND 바로 뒤에 강등 묶음을 삽입하면 다시 Topmost로 전염돼 반복 깜박이는 경로를 확인했으므로, 외부 foreground 자체가 Topmost이면 `HWND_TOP`을 기준으로 Non-Topmost 영역 내부 순서만 복원한다. 각 `SetWindowPos` 반환값과 최종 스타일을 재확인하며 권한이 낮은 실행의 거부를 성공으로 기록하지 않는다.
- **F-08 [P1, 코드 수정·자동 검증 완료/실기 대기]** 독 조작이나 Alt+Tab 없이 정상 플레이 중 작업표시줄이 갑자기 나타나는 증상도 보고되었다. 수정 전에는 폴링이 단 한 번의 `null`/최소화 표본에서 `hideAll()` 후 관리 창을 `show()`로 재생성해 자동으로 포커스를 흔들 수 있었다. 현재는 순간 이상 표본을 연속 재확인하고, 게임/설정 동기화로 생성된 브라우저·채팅·서브채팅·숙제·독 창을 `showInactive()`로 연다. 이 경로는 확정 위험이었지만 보고된 모든 증상의 단일 원인으로는 확정하지 않는다. 보스·채팅·거래·갤러리 알림은 근거 없이 비활성화하지 않고 Windows 실기 소크 테스트와 표시 사유 로그로 남은 상관관계를 확인한다.
- **F-09 [P2, 코드 수정·자동/격리 런타임 검증 완료]** 창 관리자는 800×600급 작업영역에 맞춰 계수 계산기 외곽만 줄였지만 renderer의 테이블+360px 가이드 2열 최소 폭은 유지되어 854×464 DIP probe에서 문서 폭 998px 중 183px가 잘렸다. 1000px 이하에서 상단 입력을 줄바꿈하고 가이드를 테이블 아래로 배치하며, 문서 세로·테이블 가로 스크롤을 허용한다. 816×424 renderer 회귀 검사로 가로 document overflow 제거, 실제 세로 scroll 이동, 일반 1100px 2열·360px 가이드 보존을 고정했다.

### G. 렌더러 DOM·비동기 상태

- **G-01 [P1, 코드 수정·자동 검증 완료]** 모험일지 사용자/DB 문자열은 escape된 로그 포맷과 제한된 asset 경로만 HTML 템플릿에 넣고, 삭제는 숫자 row ID `data-*`와 이벤트 리스너로 처리한다. 태그·속성 종료 문자열 fixture를 실제 DOM에 렌더링해 요소나 inline handler로 해석되지 않는지 확인했다.
- **G-02 [P1, 코드 수정·자동 검증 완료]** 일간/주간 상세가 하나의 generation을 공유해 늦은 응답을 폐기하고, 추가·삭제를 시작할 때 캡처한 날짜와 원본 row ID로만 작업한다. 날짜가 바뀐 뒤 완료된 작업은 새 화면을 다시 그리지 않는다. grouped timeline은 원본 행의 `id/source`를 유지하며 `source=manual`인 개별 행만 단건 삭제할 수 있다.
- **G-03 [P2, 코드 수정·자동 검증 완료]** 월 데이터/요약과 통계/차트 요청을 월별 generation으로 보호하고 모든 await 뒤 캡처한 월이 여전히 최신인지 확인한다. 월·탭 전환으로 무효화된 응답은 화면과 차트를 갱신하지 않는다.
- **G-04 [P2, 코드 수정·자동 검증 완료]** history/search/검색 닫기/탭 전환을 하나의 view generation으로 통합하고 캡처한 탭·검색어를 사용한다. stale success/catch/finally는 화면·검색 상태를 바꾸지 않으며, live 이벤트는 view generation과 분리해 정상 요청을 무효화하지 않는다.
- **G-05 [P1, 코드 수정·자동 검증 완료]** history/search/live/과거 prepend 데이터를 메모리 배열에 보존하고 viewport 주변 overscan 범위만 실제 DOM으로 렌더링한다. 기존 1,000/2,500개 도달 시 상단 데이터를 삭제하던 경로를 제거하고 ID 중복만 합친다.
- **G-06 [P2, 코드 수정·자동 검증 완료]** 가상 행의 실제 높이와 6px gap을 별도로 계산·캐시하고 prepend, live append, 폭·글꼴 크기 변경 뒤 같은 앵커 행의 화면 위치를 복원한다. 숨겨진 재사용 창에서도 갱신되도록 rAF와 40ms 폴백을 함께 사용한다.
- **G-07 [P2, 코드 수정·자동 검증 완료]** 갤러리 감시 키는 1~10자리 숫자 문자열만 허용하고 제목·번호·댓글 수를 DOM API와 `textContent`로 만든다. 악성 키·제목 fixture에서 잘못된 키가 제외되고 열기/삭제에는 검증된 ID만 전달되는지 확인했다.
- **G-08 [P2, 코드 수정·자동 검증 완료]** 커스텀 알림음 option은 `createElement`와 `textContent`/`value`로 생성한다. 태그·속성 종료 문자열이 포함된 이름과 파일 fixture를 실제 select에 렌더링해 DOM 요소로 해석되지 않는지 확인했다.

### H. 스케줄러·오디오

- **H-01 [P1, 코드 수정·자동 검증 완료]** resume와 unlock-screen 연속 이벤트에서 기존 분 타이머와 복귀 보정 타이머를 모두 취소하고 세대 토큰으로 마지막 보정 작업만 유효하게 유지한다. 놓친 분 이력과 현재 분 콜백도 중복 실행하지 않는다.
- **H-02 [P1, 코드 수정·자동 검증 완료]** 분 스케줄 콜백을 예외 격리된 single-flight로 실행하고 동기 예외·비동기 reject 여부와 무관하게 완료 뒤 현재 시각 기준 다음 분을 정확히 한 번 예약한다. 장기 콜백 실행 중에는 새 콜백을 겹쳐 실행하지 않는다.
- **H-03 [P2, 코드 수정·자동 검증 완료]** 사이드바 알림 큐의 Audio 인스턴스·전환 타이머를 재생 세대가 소유하게 했다. 미리보기는 이전 큐와 전환 타이머를 취소하며 이전 Audio의 늦은 종료·reject 콜백은 새 재생을 정지하거나 교체하지 못한다.
- **H-04 [P2, 코드 수정·자동 검증 완료]** 동일 파일 800ms throttle 경과시간을 문서 시계와 독립적인 `performance.now()`로 계산한다.
- **H-05 [P2, 코드 수정·자동 검증 완료]** 보스 analytics에 `YYYY-MM-DD HH:mm + 보스명` 안정 키를 적용해 잠금 해제·복귀로 같은 분을 다시 검사해도 보스별 이벤트를 한 번만 기록한다.
- **H-06 [P3, 코드 수정·자동 검증 완료]** 보스 알림·analytics Set 정리 날짜를 `YYYY-MM-DD`로 비교해 월·연도 경계에서도 이전 키를 제거한다. 두 Set의 합계가 500개를 넘는 비정상 상태에서도 함께 정리한다.
- **H-07 [P3, 코드 수정·자동 검증 완료]** `BUFF_USED` 리스너를 안정 참조로 보관하고 `buffTimerManager.stop()`에서 제거한 뒤 `_started=false`로 되돌린다. 중복 start는 리스너를 추가하지 않고 stop 뒤 start는 정확히 한 개를 다시 등록한다.
- **H-08 [P3, 코드 수정·자동 검증 완료]** 사운드 throttle Map에서 800ms가 지난 항목을 정리하고 최대 128개만 유지한다.

## 4. 확인 완료된 기존 방어

다음 항목은 현재 변경에서 요구 취지대로 구현돼 있으므로 회귀 테스트만 보강하고 재설계하지 않는다.

- refresh token 중복 갱신 락과 `finally` 해제
- OAuth refresh endpoint 400/401 무효화
- OAuth/Drive 요청 15초 제한
- 프로토콜 파일명의 `path.basename` 경로 탈출 방어
- 보스별 Set 키를 이용한 동일 분 복수 보스 알림
- 짧은 버프의 즉시 사전 경고 생략 및 로그 시간 NaN fallback
- 실시간 Tail 줄 단위 멀티바이트 경계 보존
- 갤러리 감시글 라운드로빈과 정규식 메타문자 escape
- `execSync`의 `windowsHide: true` 및 stderr 억제
- polling의 `!isVisible` 제거와 안정 상태 1초 폴링 전환
- `OpenProcess` 성공/실패/조기 반환의 `finally` CloseHandle
- 일반 게임 rect/해상도 변화 감지와 DIP 변환
- 채팅 Set 기반 탐색·클릭 이벤트 위임
- 채팅/단어 알림/거래/원격 갤러리의 주요 텍스트 escape
- 파일별 800ms 사운드 throttle, 서로 다른 사운드 큐잉, 토스트 유지
- 월간 SQLite `LIKE` 제거와 range index 사용
- WAL autocheckpoint 1000 및 DB close 시 statement cache 정리
- XP 주요 합계의 0 하한과 득템 이름 캐시

## 5. Phase 1 ~ Phase 6 구현 순서

### Phase 1 — 데이터 안전망·설정 원자성·보안 경계 (완료)

대상: A-01~A-06, B-01, D-07~D-08, G-01, G-07~G-08

1. 설정 read/write 경계를 깊은 복사 또는 불변 업데이트로 전환한다.
2. 설정 저장을 임시 파일 쓰기 → 파일 flush → 원자 rename 방식으로 변경한다.
3. 저장 실패 시 pending을 유지하고 제한적 재시도/종료 보고가 가능하게 한다.
4. 설정 로드는 `사용자 값 보존 + 신규 기본 키만 추가` 정책으로 바꾼다. 기존 키는 `false`, `0`, 빈 문자열, 빈 배열을 포함해 절대 기본값으로 덮지 않고, 알려진 중첩 객체에는 누락된 하위 키만 재귀 추가한다. 배열은 일반 딥 머지하지 않으며 기본 퀵슬롯 추가 같은 제품 변경은 버전이 있는 1회 마이그레이션으로만 수행한다.
5. v3 진입 버전 센티널과 일관된 DB 스냅샷 백업을 구현한다.
6. 레거시 데이터는 삭제 전에 전체 충돌 백업과 복사 검증 매니페스트를 남긴다.
7. `contents.json`에 배열·필수 필드·ID 중복·sentinel·리소스 버전 검증을 추가하고 검증 실패 시 파괴적 병합을 전면 중단한다.
8. IPC 스키마 검증과 프로덕션 테스트 API 차단을 적용한다.
9. 모험일지·갤러리·사운드 옵션의 외부 문자열을 DOM API/textContent로 전환하고 inline handler를 이벤트 위임으로 교체한다.
10. 우선 기존 호출자 호환성을 위해 `load()`마다 독립 스냅샷을 반환한다. Phase 4에서 실제 프로파일링 후 읽기 전용 API를 별도로 도입할 수 있으며, 기존 수정 패턴을 전수 전환하기 전에는 동결 객체를 반환하지 않는다.
11. 미인식 설정 키는 실행 객체에 그대로 섞지 않고 별도 quarantine 영역에 보존해 하위 호환성과 보안을 분리한다.
12. 스키마 오류는 설정 전체를 폐기하지 않고 필드별로 격리·기본값 복구하며 사용자에게 복구 로그를 남긴다.
13. 자동 v3 백업과 수동 백업/복원을 공통 SQLite snapshot 서비스로 통합한다.

사이드이펙트 방어:

- 백업 완료 플래그는 모든 필수 항목 성공 뒤 원자적으로 기록한다.
- 중첩 설정 merge는 기존 사용자 값을 불변으로 두고 누락된 신규 기본 키만 추가한다. 배열과 사용자 순서가 있는 목록은 일반 병합하지 않으며, 동적 맵의 신규 내장 항목만 안정적인 ID 기준으로 추가한다.
- Windows의 백신·OneDrive 잠금을 고려해 동일 디렉터리 임시 파일, 파일 flush, 제한 재시도, 시작 시 유효본 선택을 구현하고 성공 전 원본은 삭제하지 않는다.
- SQLite snapshot은 DB를 열거나 어떤 마이그레이션을 실행하기 전에 생성하며 백업 형식·버전 메타데이터를 자동/수동 백업에서 동일하게 유지한다.
- DOM 변경 전후 동일 데이터 렌더 스냅샷과 악성 문자열 fixture를 추가한다.
- inline handler 제거 후 클릭·Enter·Space, CSS class/data 속성, 이벤트 전파가 동일한지 검사한다.
- Phase 종료 게이트: `npm run typecheck`.

### Phase 2 — SQLite·체크리스트 상태 무결성 (완료)

대상: B-02~B-09, C-01~C-14

1. 모든 버전 마이그레이션을 버전별 단일 트랜잭션으로 만들고 성공 뒤에만 `user_version`을 올린다.
2. DB 초기화 시 사냥터 동선 시뮬레이션 기본 지도 3개의 이름·이미지 경로·zoom/좌표 변환 메타데이터를 `INSERT OR IGNORE`로 보장한다. 기존 행과 동선 점은 덮어쓰지 않는다. 잘못 변환 가능한 조/억/만 금액과 레거시 숙제 ID도 버전 마이그레이션으로 복구한다.
3. DB 초기화 실패 시 statement cache와 연결을 정리하고 재시도 가능한 상태로 되돌린다.
4. 공개 DB 쓰기 API에 commit 이후 성공 결과와 명시적 실패 결과를 도입한다.
5. 배치 rollback 성공 오보를 제거하고 동기화 상위 계층까지 실패를 전파한다.
6. activity 삭제를 DB row id 기반 단건 삭제로 변경하고 UI/IPC/preload 계약을 함께 갱신한다. `source=manual|automatic|legacy-unknown`을 저장해 새 수동 기록만 삭제 버튼을 표시하며, 출처를 확정할 수 없는 레거시 기록은 보존 우선으로 자동 삭제 대상에서 제외한다.
7. 삭제 식별자와 집계 키를 분리한다. row ID는 삭제할 한 건만 지정하고, 중복 아이템 합계·월간 누적은 기존처럼 날짜/type/canonical item과 `SUM(amount)`로 계산한다. 한 행 삭제 후에는 남은 행으로 그룹 합계와 통계를 재계산한다.
8. 엘소 flush 실패를 호출자에게 전달하고 종료 복구 정책을 연결한다.
9. hot-path 상수 SQL만 준비문 캐시에 넣고 동적/마이그레이션 SQL은 캐시하지 않는다.
10. 금액·복수 아이템·외치기 중복 파서 fixture를 추가한다.
11. `contentsChecker.init()`을 정상 시작 경로에 1회 호출하고 멱등성을 테스트한다.
12. 레거시 ID를 먼저 canonical ID로 정규화한 뒤 상태를 최신 시각/리셋 경계 기준으로 병합한다.
13. 숙제 `pending`은 리셋 경계 전에 폐기하고 이벤트 순서를 보존한다. 증가 패턴은 현재 횟수에 더하고, “이번 주 도전 횟수 N회” 같은 횟수 감지는 그 시점의 절대 횟수 N으로 설정한다. 이후 도착한 증가 이벤트만 추가한다.
14. 가시성 기본값을 `!== false`로 통일한다.
15. 모든 DDL/마이그레이션이 끝난 뒤에만 statement cache를 시작하고 스키마 변경 전후 캐시를 명시적으로 비운다.
16. `1조` 복구는 `amount === 1` 같은 숫자 조건이 아니라 원문에 조/억/만 단위가 존재하는 영향 행만 재파싱한다.
17. 숙제 중복 제거는 단순 최소/최대 ID 삭제가 아니라 필드별 canonical 병합 결과를 만든 뒤 UNIQUE를 추가한다.
18. 엘소 recovery journal은 연산 ID와 commit marker를 가지며 재시작 시 이미 커밋된 연산을 다시 적용하지 않는다.
19. pending operation에는 source event ID, 리셋 주기, 크기 상한을 두어 로그 재처리 중복과 무한 성장을 차단한다.
20. 숙제 로그는 `(date, content_id)` 기준 한 건을 유지하고 재완료 시 `completed_at`, 이름, category, type을 함께 UPSERT한다. 콘텐츠 1회 초기화권 사용 여부는 별도 로그가 없으므로 별도 완료 행을 만들지 않고 최신 완료만 표시한다.
21. XP 음수 이벤트는 사망 손실로 해석하지 않는다. 경험의 정수 1개당 정확히 100억 XP 교환으로 계산해 정수 개수와 교환 후 누적량을 갱신하고, 세션 총 획득 XP는 유지하며 교환으로 설명되지 않는 작은 음수는 진단 로그 후 무시한다.

사이드이펙트 방어:

- v2 마이그레이션 전 DB 백업을 강제한다.
- 파괴적 dedupe는 어떤 행을 보존했는지 로그/테스트로 결정 가능하게 한다.
- 기본 지도 보장은 지도 삭제 UI가 없는 현재 제품 계약에 한해 매 초기화에서 멱등적으로 실행한다. `INSERT OR IGNORE`로 기존 지도 메타데이터와 사용자가 그린 동선을 변경하지 않는다.
- 동일 아이템 중복 행 fixture에서 row ID 한 건 삭제 전후의 일간·월간 합계가 삭제한 행의 `amount`만큼만 변하고 나머지 행이 계속 합산되는지 검증한다.
- recovery journal 재생은 같은 연산을 두 번 실행해도 결과가 한 번만 반영되는 멱등성 테스트를 통과해야 한다.
- DB API 반환형 변경은 모든 호출부를 함께 수정해 성공 UI 오보를 막는다.
- DB 쓰기 실패를 catch한 뒤 성공처럼 계속하지 않고 설정-DB 불일치를 명시적 실패/재시도 상태로 노출한다.
- Phase 종료 게이트: `npm run typecheck`.

### Phase 3 — 클라우드 큐·인증·종료 상태 머신

대상: A-07~A-09, B-05, D-01~D-06, D-09~D-11

진행 상태: 분리 파일 전송 큐·파일별 dirty/debounce·meta ID/generation·숙제 base snapshot/로컬 outbox/3방향 병합·업로드 후 operation 재조회 확인/누락 operation mutation 재실행·회사/집 pull 루프·절전/게임/네트워크 복구 즉시 pull·installation jitter/지수 백오프·OAuth 로그인 세대 차단·401 재인증 및 요청 취소를 연결했다. 메모리 Drive 통합 검사로 분리 업로드, 회사→집 숙제 수신, 파생 echo outbox 방지, 교차 PATCH overwrite, 동일 숙제 완료/해제/횟수 충돌, 응답 유실과 재시작 재수렴을 통과했다. 새 PC 프로필 판정, 유효 중복 파일 선택, 파일별 선택·독립 복원·부분 결과, 값 비노출 변경 요약, 로컬 백업 되돌리기와 파일별 checksum/revision/dirty/retry UI도 자동 검증했다. 종료 시 창·트레이를 먼저 숨기는 단일 finalizer, 최대 3초 drain, 파일별 recovery marker, 응답 유실 후 다음 실행 재확인, 두 번째 quit 차단, WAL checkpoint와 Windows 세션 종료 fast path까지 자동 검증했다. 실계정 2-PC 및 실제 Windows 종료·로그오프 실기 검증은 남아 있다. 미배포 기능이므로 개발 중 단일 파일은 마이그레이션 대상이 아니다.

1. 클라우드 정식 저장 계약을 처음부터 다음 세 파일로 구성한다. 개발 중 생성된 `tw_overlay_sync.json`은 조회하지 않는다.
   - `tw_overlay_settings.json`: 일반 설정의 클라우드 권위 스냅샷
   - `tw_overlay_checklist.json`: 숙제 정의·캐릭터·리셋 주기별 진행 상태·tombstone
   - `tw_overlay_sync_meta.json`: 스키마 버전과 두 데이터 파일의 Drive file ID·종류·생성 세대
2. 각 데이터 파일은 독립적인 `schemaVersion`, `revision`, `updatedAt`, `updatedByInstallationId`, `checksum`을 가진다. 메타 파일은 데이터 파일이 새로 생성·교체·마이그레이션될 때만 갱신하고 매 설정/숙제 저장마다 쓰지 않아 단일 병목을 다시 만들지 않는다.
3. appDataFolder의 파일명은 유일하지 않으므로 메타 파일의 Drive file ID를 우선 사용한다. 새 PC의 최초 탐색은 파일명과 앱 전용 종류 표식을 함께 조회하고, 중복이 있으면 revision·checksum 검증을 통과한 최신 세대만 선택한다. 임의의 첫 파일을 적용하지 않는다.
4. 업로드/복원/미리보기를 구분하는 전역 Drive 전송 큐를 두되, `settingsDirty`, `checklistDirty`, `metadataDirty`와 재시도 상태는 분리한다. 한 종류 실패가 다른 종류의 저장 완료를 실패로 바꾸지 않으며 전송 큐는 single-flight와 공정한 drain 순서를 보장한다.
5. 일반 설정 저장에서 동기화 대상 비숙제 필드가 실제로 바뀐 경우에만 `settingsDirty`를 세운다. 연속 슬라이더·텍스트 입력은 마지막 변경 후 1~2초 debounce하여 한 번에 합치고, blur·명시적 저장·종료 flush에서는 즉시 큐에 넣는다. 입력 한 글자마다 Drive 요청을 보내지 않는다.
6. 채팅 로그 자동 감지, 사용자의 완료·해제·N/A·횟수 변경, 캐릭터 미선택 상태의 `pendingHomeworks`, 커스텀 숙제/캐릭터 정의 변경은 로컬 영속화가 성공한 뒤 안정적인 operation ID를 로컬 outbox에 먼저 기록하고 `checklistDirty`만 세운다. 일반적인 단일 감지는 즉시 또는 500ms 이내 업로드를 시작하고, 같은 로그 묶음에서 연속 감지되면 한 payload로 합친다.
7. 설정 변경은 설정 파일만, 숙제 이벤트는 숙제 파일만 PATCH한다. 수동 전체 동기화·새 PC 복원만 두 파일을 모두 조회하며 한 파일의 변경 때문에 다른 파일을 재업로드하지 않는다.
8. 로그인 직후에는 bootstrap download/merge가 끝날 때까지 로컬 자동 업로드를 잠근다. 설정 파일이 있으면 먼저 적용하고, 없을 때만 현재 로컬 설정을 최초 클라우드 기준값으로 업로드해 오래된 PC가 신규 클라우드 값을 덮는 시작 순서 경쟁을 막는다.
9. 클라우드에서 내려온 설정을 로컬에 저장하는 동안에는 origin token으로 변경 감지를 억제해 같은 값을 즉시 재업로드하는 echo loop를 막는다. 숙제 병합 적용도 결과가 원격과 다른 로컬 우선 충돌/outbox를 포함할 때만 후속 업로드한다.
10. 서로 다른 수동 작업은 현재 Promise를 재사용하지 않고 명시적 busy 결과를 반환한다. 작업 큐는 현재 작업 내부에서 자기 작업 결과를 기다리지 않도록 dirty 플래그를 외부 drain loop가 소비하게 한다.
11. 로그아웃/인증 무효화/종료가 공유 AbortController로 진행 요청을 취소하게 한다. Drive 401은 1회 refresh/retry 후 인증 무효화하고 UI/config 상태를 함께 갱신한다.
12. OAuth 세대 토큰으로 취소/만료된 로그인 세션의 늦은 토큰 저장을 차단한다. access token이 아직 유효하면 refresh token 없이도 사용하고, 만료 시에만 재로그인을 요구한다.
13. 세 payload 모두 크기, 최상위 형태, schemaVersion, revision, checksum, 필드 타입과 종류별 allowlist를 로컬 저장 전에 검증한다.
14. 숙제 상태는 마지막 정상 동기화본을 base로 둔 3방향 병합을 수행한다. 키는 `contentId + characterId + resetCycle`이며 `로컬만 변경=로컬`, `클라우드만 변경=클라우드`, `양쪽 동일 변경=그 값`, `양쪽 충돌 변경=실제 게임을 추적한 로컬`로 결정한다. 단순히 같은 키가 존재한다는 이유만으로 오래된 로컬 상태가 최신 클라우드 상태를 막지 않게 한다.
15. 완료 해제·N/A 해제·횟수 감소·커스텀 항목 삭제도 명시적 tombstone/operation으로 보존한다. operation에는 배열 인덱스가 아닌 숙제·캐릭터 안정 ID 경로의 `before/after` mutation을 포함한다. 체크리스트 업로드는 `원격 재조회 → base/local/remote 병합 → revision 증가 업로드 → 재조회 검증`으로 수행하고, 자신의 operation ID가 원격에서 확인될 때까지 outbox에서 제거하지 않는다. 다른 PC의 동시 PATCH로 사라지면 누락 mutation만 최신 원격 위에 재실행하고 다시 업로드해 비충돌 변경과 두 operation ID를 함께 보존한다.
16. tombstone과 operation은 최소 현재·직전 리셋 주기를 보존한다. 오래된 주기 정리는 현재 주기 판정과 서버 revision 반영이 확인된 뒤 수행해 느리게 접속한 PC가 삭제/해제를 되살리지 못하게 한다.
17. 캐릭터 프리셋·커스텀 숙제 정의는 안정 ID로 합집합 병합한다. 같은 ID의 이름·분류·리셋 시각 같은 정의 충돌은 클라우드 값을 사용하되 진행 상태는 3방향 병합 규칙을 따른다. 같은 ID인데 생성 origin/fingerprint가 다른 실제 충돌은 한쪽에 새 ID를 발급하고 참조를 함께 옮긴다.
18. 일반 설정은 병합하지 않고 클라우드에 존재하는 필드를 권위 스냅샷으로 교체한다. 알림 on/off·볼륨·기본 사운드 ID·단축키·키워드·퀵슬롯·사용자 정의 알람/채팅 탭·UI 옵션 등이 대상이다. 적용 직전 원자적 로컬 백업을 만들고 변경 요약과 되돌리기 진입점을 제공한다.
19. 동기화가 꺼져 있거나 오프라인인 동안 바꾼 로컬 일반 설정은 재연결 시 최신 클라우드 설정에 의해 되돌아갈 수 있다. 이를 제품 계약으로 명시하고 `cloudRevision/baseCloudRevision`으로 판정한다. 동기화된 상태에서 새로 발생한 로컬 설정 변경은 다음 클라우드 revision으로 업로드한다.
20. `positions`, `storedPositionKeys`, 게임/채팅 로그 경로, 모니터·DPI 의존 창 크기/좌표, OAuth 토큰, Discord webhook URL, 설치 표식, DB/채팅/알람 이력과 사용자 PC의 절대 경로인 커스텀 사운드는 클라우드 파일에서 제외한다. 내장 사운드는 이식 가능한 asset ID만 저장한다. Discord webhook URL은 메시지 전송 권한을 가진 비밀 자격증명이므로 PC방·공용 PC의 클라우드 복원으로 노출되지 않게 각 PC 로컬에만 유지하고, 로그·오류 메시지·미리보기·진단 내보내기에서도 전체 값을 마스킹한다.
21. 새 PC에서는 클라우드를 초기 로컬 기준값으로 허용한다. 앱 데이터 생성 전에 로컬 전용 설치 메타데이터(`installationId`, `createdAt`, `profileState`)와 기존 `config.json`/`diary.db` 존재 여부를 검사한다. 모두 없던 프로필만 `fresh`로 판정하고, 일부 파일이 있거나 손상되어 애매하면 `established/needs-confirmation`으로 두어 자동 덮어쓰기를 금지한다. 설치 표식은 클라우드에 올리지 않는다.
22. `fresh` 프로필에서 메타→설정→숙제 파일 순으로 검증·적용한다. 설정은 클라우드 스냅샷, 숙제는 로컬이 비어 있으므로 클라우드 기준 상태를 적용하며 각 파일에 없는 신규 앱 기본 키만 추가한다. 어느 한 파일이 없거나 손상돼도 정상인 다른 파일은 독립 복원하고 부분 복원 상태를 UI에 표시한다.
23. 명시적 복원은 설정/숙제를 각각 선택 가능하게 한다. 설정 복원은 클라우드 필드 교체, 숙제 복원은 3방향 병합이며, 실행 전에 파일별 변경 미리보기와 로컬 백업을 만든다.
24. 개발 중 단일 `tw_overlay_sync.json`은 정식 입력으로 취급하지 않고 조회·분할·병합·재업로드하지 않는다. 파일명에는 스키마 버전을 붙이지 않고 각 JSON 내부의 `schemaVersion`으로만 호환성을 판별한다.
25. abort된 업로드도 서버에 반영됐을 수 있으므로 다음 실행에서 checksum/modifiedTime/revision을 조회해 성공 여부를 조정한다.
26. 종료를 `정지 요청 → 모든 창/트레이 즉시 숨김 → 신규 dirty 생산 정지 → config/outbox flush → 두 클라우드 dirty 큐 최대 3초 drain/취소 → WAL checkpoint → DB close → 최종 quit` 단일 상태 머신으로 만든다. 시간 초과 시 파일 종류별 recovery marker를 남겨 다음 시작에 조용히 재시도한다.
27. 외부 두 번째 quit는 계속 막고 finalizer만 최종 종료를 허용한다. Windows 로그오프·시스템 종료처럼 긴 대기가 불가능한 경로는 원자적 recovery marker를 먼저 기록하는 fast path로 분리한다.
28. 로그인된 다른 PC의 원격 변경을 받기 위한 pull 루프를 파일별 revision/modifiedTime 기반으로 둔다. 게임 실행 중이거나 숙제 창이 열려 있을 때는 약 30초 간격, 게임이 꺼진 유휴 상태에서는 약 5분 간격으로 가볍게 변경 여부만 확인하고 실제 revision이 바뀐 파일만 다운로드·검증·적용한다. 모든 주기에는 installation별 jitter를 넣고 429/5xx/오프라인은 지수 백오프한다.
29. 앱 시작·로그인 완료·절전 복귀·네트워크 재연결·게임 프로세스 최초 감지 시에는 주기 타이머를 기다리지 않고 설정/숙제 원격 revision을 즉시 확인한다. 특히 게임 감지 직후에는 체크리스트 bootstrap pull/merge가 완료되거나 제한 시간으로 오프라인 판정될 때까지 새 숙제 자동 감지를 짧게 대기열에 보존해 오래된 집 PC 상태 위에 먼저 적용하지 않는다.
30. 원격에서 받은 숙제의 cloud-only 변경은 `base/local/remote` 병합 후 로컬에 즉시 반영하고 숙제 창·오늘 요약을 갱신하되, origin token으로 echo upload를 만들지 않는다. 로컬 outbox가 동시에 있으면 원격 재조회→3방향 병합→operation 재게시 절차로 수렴시키며, 단순히 마지막 업로드 시각이 늦다는 이유만으로 회사/집 어느 PC 전체를 우선하지 않는다.
31. pull 루프의 마지막 확인 page token/revision과 파일별 base snapshot은 로컬에 원자 저장한다. 앱이 며칠간 켜져 있거나 절전·네트워크 단절을 반복해도 누락 구간을 다음 즉시 pull에서 재조회하고, 한 PC가 원격 변경을 적용한 사실만으로 설정/숙제 파일을 다시 쓰지 않는다.

클라우드 데이터 분류:

| 물리 파일/로컬 영역 | 데이터 예시 | 변경 시 동작 | 복원·충돌 정책 |
|---|---|---|---|
| `tw_overlay_settings.json` | 알림·볼륨·내장 사운드 ID·단축키·키워드·퀵슬롯·사용자 알람·채팅 탭·UI 옵션 | 설정 변경을 1~2초 합쳐 설정 파일만 업로드 | 클라우드에 있는 필드는 클라우드 스냅샷으로 교체. 적용 전 로컬 백업 |
| `tw_overlay_checklist.json` | 커스텀 숙제·캐릭터·완료·해제·N/A·횟수·완료 시각 | 로컬 commit+outbox 후 즉시/최대 500ms 내 숙제 파일만 업로드 | 안정 ID와 리셋 주기 기준 3방향 병합. 양쪽 충돌만 로컬 우선, tombstone/operation으로 수렴 |
| `tw_overlay_sync_meta.json` | 파일 ID·종류·스키마·생성 세대 | 파일 생성·교체·마이그레이션 때만 업로드 | 새 PC 탐색과 중복 파일 판별에 사용. 매 데이터 변경의 revision 저장소로 사용하지 않음 |
| PC 종속·민감 로컬 영역 | 창 좌표·모니터/DPI·로그 경로·설치 표식·OAuth·Discord webhook URL·절대 사운드 경로 | 클라우드 dirty를 만들지 않음 | 각 PC 로컬 유지. Webhook URL은 로그·미리보기·진단에서도 마스킹 |
| 실행 이력·원본 데이터 | 일지 DB·채팅 로그·알람 이력 | v3 설정 동기화 대상 아님 | 향후 별도 백업 기능으로만 다룸 |

사이드이펙트 방어:

- 세 파일의 스키마 버전을 독립적으로 올리고 각 파일의 독립 round-trip·손상·부분 부재 테스트를 추가한다.
- 큐 테스트는 설정 debounce 중 숙제 감지, 설정 upload 중 숙제 dirty, 숙제 upload 중 설정 변경, restore 중 dirty update, logout 중 fetch, 종료 중 두 번째 quit를 포함한다.
- 설정 변경이 숙제 파일을 쓰지 않고 숙제 감지가 설정 파일을 쓰지 않는지 Drive mock의 file ID별 호출로 검증한다.
- 클라우드 적용 origin token이 저장 이벤트를 다시 발생시키지 않는지, 연속 UI 입력 100회가 한두 번의 설정 업로드로 합쳐지는지 검사한다.
- 두 PC가 같은 숙제 키를 완료/해제/횟수 변경하는 모든 3방향 조합에서 operation이 유실되지 않고 재조회 후 같은 결과로 수렴하는지 검증한다.
- 회사·집 PC를 각각 독립 설치 ID와 base snapshot으로 실행하는 테스트를 추가한다. 회사 PC에서 숙제 완료→500ms 내 업로드→집 PC 유휴 pull 또는 게임 시작 즉시 pull→집 PC UI 반영→echo upload 없음의 전 과정을 검증한다.
- 집 PC가 절전 중이거나 네트워크가 끊긴 동안 회사 PC에서 여러 숙제를 완료한 뒤, 집 PC 복귀/게임 시작 시 중간 revision을 놓치지 않고 최종 상태로 수렴하는지 검증한다.
- 회사 PC 업로드와 집 PC의 로컬 완료가 같은 시점에 교차하는 fixture에서 양쪽 operation ID가 최종 원격 payload와 두 로컬 상태에 모두 남고, 이후 아무 조작 없이 pull만으로 같은 결과가 되는지 검증한다.
- **자동 검증 완료(2026-08-25):** 서로 다른 캐릭터 상태의 교차 변경과 같은 숙제의 완료/해제/횟수 충돌 fixture가 같은 로컬 결과로 수렴하고 두 operation ID를 최종 payload에 보존했다. 실제 매니저+메모리 Drive에서는 확인 직후 overwrite→누락 mutation 재게시, 응답 유실 뒤 중복 업로드 없는 확인, `cloud-sync-state.json` 재로드 뒤 재수렴을 통과했다. 실계정 2-PC 검증은 별도로 남긴다.
- **교차 병합 stress 자동 검증 완료(2026-08-26):** `8f643f1`에서 완료·해제·횟수 0~3, 동일 필드 충돌과 서로 다른 캐릭터 변경, 회사/집 operation 생성 시각과 입력 순서를 바꾼 256개 결정론적 조합을 반복했다. operation 배열 순서를 반대로 넣어도 최종 원격 상태가 동일했고 비충돌 변경은 양쪽 모두 보존됐으며, 각 PC가 자기 업로드 확인본을 base로 다시 pull한 로컬 결과도 일치했다. 모든 최종 payload가 schema/checksum 검증을 통과하고 두 operation ID를 유지했다.
- **동시 main 프로세스 자동 검증 완료(2026-08-26):** `9974c5a`에서 회사/집 역할의 독립 userData·device ID를 가진 실제 `main.js` 두 프로세스를 동시에 시작했다. 첫 숙제 다운로드를 같은 원격 revision에 배리어로 맞춘 뒤 서로 다른 캐릭터의 완료·횟수 변경을 교차 업로드해 최초 두 업로드가 서로를 overwrite하도록 강제했다. 누락 operation 재게시로 총 3회 이상 전송된 뒤 최종 원격 payload와 양쪽 `cloud-sync-state.json` 확인 이력에 두 operation ID가 남고, 양쪽 로컬 숙제 상태가 동일하게 수렴하며 outbox가 비워지는 것을 확인했다. `ab443e9`에서는 수렴한 회사·집 userData를 각각 새 `main.js` 프로세스로 다시 열어 두 operation 확인 이력과 두 숙제 변경이 유지되고 outbox가 빈 상태이며, 두 재시작 모두 변경 없는 echo upload를 추가하지 않는 것까지 확인했다. `2fe2491`에서는 같은 숙제·캐릭터의 동일 횟수 필드에 회사 1회·집 2회 operation을 교차시켜 필드별 결합·수렴을 고정했다. `5043162`의 기준 캐릭터 기록 부재 fixture는 반복 게이트에서 앱 초기화가 정상 기본값 `false/0`을 보정하고 파생 operation을 만드는 비정상 전제로 판명돼 정상 기준 상태로 바로잡았다. 이 반복에서 두 operation ID는 모두 남아도 마지막 업로더와 로컬 우선 병합 순서에 따라 횟수 1/2가 달라지는 실제 결함을 재현했다. `8695630`은 업로드 전과 수신 3방향 병합 후 전체 확인/outbox operation을 결정적 순서로 재생해 canonical data를 사용하도록 수정했다. 이후 전체 게이트를 연속 두 번 실행해 최종 원격·양쪽 로컬이 완료·횟수 2·시각 20000으로 일치하고 두 ID 보존, outbox 정리, 양쪽 재시작 무 echo를 확인했다. Windows 테스트 잠금 디렉터리 경합의 일시 `EPERM`은 `5043162`의 제한 재시도를 유지한다. 실제 Google Drive와 두 Windows PC의 네트워크·poll 시각 검증은 별도로 남긴다.
- **클라우드 pull scheduler 자동 검증 완료(2026-08-26):** `fb49591`에서 로그인·자동 동기화가 활성화된 `established` 프로필의 실제 `cloudSyncManager`를 실행하고 게임 상태만 `running`/`stopped`로 전환했다. 같은 installation ID의 결정적 jitter를 포함해 게임 실행 상태는 27~33초, 미실행 상태는 270~330초 범위의 다음 pull 타이머를 각각 정확히 하나 예약하고 중지 시 예약을 정리하는 것을 확인했다. `055dfbd`에서는 게임 실행 중 Drive 목록 조회 실패를 주입해 첫 재시도가 60초 기준 54~66초 범위로 증가하고, 다음 정상 pull 성공 뒤 27~33초 범위로 초기화되는 실패 backoff도 실제 매니저 경로로 검증했다. `54f9320`은 정상 장기 타이머가 있는 상태의 즉시 pull이 기존 예약을 취소하고 Drive 목록을 바로 조회한 뒤 정상 27~33초 후속 타이머 하나만 재예약하는 경계를 고정했다. pull 중 생성된 dirty를 `flushPendingSync()`가 정리하면 후속 업로드용 목록 조회가 추가될 수 있다. `4021977`은 앱 시작, 절전 복귀, 잠금 해제, 10초 주기의 네트워크 오프라인→온라인 전환, 게임 최초 시작, 자동 동기화 활성화가 각각 즉시 pull 요청에 연결된 계약을 정적 회귀 검사로 고정했다. `fc25244`는 Google 로그인 성공 직후 scheduler를 시작하고 최초 `syncFromCloud(false)`를 기다리는 연결을 추가로 고정했다. 실제 두 PC와 Google Drive에서의 원격 변경 도착 시각과 Windows 이벤트·계정 로그인 발생은 실기로 남긴다.
- **자동 검증 완료(2026-08-25):** 앱 데이터 fixture에서 `fresh`/`established`/`needs-confirmation`을 구분하고, `needs-confirmation` 자동 복원을 차단했다. 최신 메타가 손상된 중복 파일은 이전 유효 메타가 가리키는 checksum 통과 파일로 대체했으며, 설정만·숙제만 존재하거나 generation이 다른 경우에도 정상 파일을 독립 복원하고 나머지를 `missing`/`generation-mismatch`로 분리 보고했다. Electron 렌더러 검사에서 설정/숙제 선택값 전달과 부분 복원 결과 표시를 통과했다.
- **자동 검증 완료(2026-08-26):** 실제 빈 userData 격리 실행에서 기본 숙제 초기화가 `fresh` outbox를 만드는 결함을 재현했다. 수정 후 로그인 전·로그인 직후 `fresh` 초기화는 outbox를 만들지 않고, `established` 프로필의 오프라인 변경은 operation으로 남는 회귀 검사를 통과했다.
- **부분 복원 main 재시작 검증 완료(2026-08-26):** `b1fbef7`에서 손상된 설정·정상 숙제·손상된 메타를 가진 지속형 모의 Drive와 실제 `main.js`를 연결했다. fresh 시작은 정상 숙제만 독립 복원하고 `needs-confirmation`으로 전환했지만, 수정 전 다음 시작의 파생 변경이 사용자 확인 전에 원격 다운로드와 숙제·메타 업로드를 실행하는 결함이 재현됐다. 자동 전송 게이트에 확인 대기 상태를 추가한 뒤 같은 userData 재시작은 2.2초 동안 Drive 다운로드·업로드 0회를 유지하고 로컬 `userServer=7`과 원격 `userServer=16`을 모두 보존했다. `e7f7b57`에서는 설정만 선택한 수동 복원 직후 숙제·캐릭터가 유지되고 숙제 업로드가 발생하지 않으며, `established` 전환 뒤 새 설정 변경이 1.5초 debounce로 다시 업로드되는 반대쪽 경계도 고정했다. `26d95c2`에서는 정상 설정·손상된 숙제의 역방향 fresh 시작도 실제 메인 프로세스로 실행해 설정 `restored`, 숙제 `invalid`, `needs-confirmation`을 확인했고, 원격 `userServer=16`만 적용되는 동안 기존 로컬 캐릭터와 무전송 상태가 보존됨을 검증했다. `0c5d6f0`에서는 별도 userData로 설정·숙제를 모두 수동 복원한 다음 복원 전 백업으로 되돌려 로컬 설정과 캐릭터가 함께 원상복구되고, 되돌린 상태가 두 원격 파일에 각각 1회 재전송된 뒤 settings dirty와 checklist outbox가 모두 정리되는 것을 확인했다.
- **자동 검증 완료(2026-08-25):** 개발 중 단일 `tw_overlay_sync.json`을 함께 발견해도 읽거나 변경하지 않고 세 정식 파일만 사용하는 것을 통합 검사로 고정했다. 실제 설정 복원 경로에서 클라우드에 없는 `false` 사용자 값, Discord Webhook URL, 로그 경로, 창 위치와 커스텀 사운드가 유지되고 복원 전 config가 백업되는 것을 확인했다.
- **자동 검증 완료(2026-08-25):** 값 없이 추가/변경/현재 PC 유지 키만 반환하는 파일별 미리보기, 선택 파일 검증 후 복원 확인, 손상·과대 백업 거부, 최근 백업 되돌리기와 파일별 checksum/revision/dirty/retry 표시를 통과했다. 설정과 숙제를 같은 pull에서 적용해도 최초 파일 직전에 백업을 한 번만 생성해 전체 적용 전 상태로 되돌릴 수 있다.
- **자동 검증 완료(2026-08-25):** 서버가 숙제 업로드를 반영한 직후 응답이 유실된 fixture에서 outbox와 파일별 recovery marker가 유지되고, 로컬 상태 캐시를 재로드한 다음 실행에서 원격 operation을 확인해 중복 업로드 없이 둘을 제거했다. 설정 dirty key와 숙제 operation은 각각 확인된 파일만 marker에서 독립 제거된다.
- **격리 프로세스 재시작 검증 완료(2026-08-26):** `0884202`에서 실제 별도 Electron 프로세스로 설정 dirty만, 숙제 outbox만, 두 파일 모두 dirty인 세 상태를 각각 만들고 recovery marker를 저장했다. 새 Electron 프로세스가 같은 userData를 다시 열었을 때 dirty key, outbox operation ID와 파일별 marker가 모두 동일하게 유지됐다. 실제 Drive 응답 유실 종료와 원격 재확인은 실계정 실기로 남긴다.
- **격리 main finalizer 검증 완료(2026-08-26):** `65ea23d`에서 표시 창이 있는 실제 `main.js`를 설정 dirty만, 숙제 outbox만, 두 파일 모두 dirty인 세 격리 프로필로 각각 실행해 일반 종료시켰다. 세 경우 모두 창 숨김 100ms·전체 종료 3초 제한 안에서 의도한 dirty key와 operation ID를 파일별 recovery marker에 보존하고 WAL checkpoint와 DB close를 완료했다.
- **격리 Drive timeout finalizer 검증 완료(2026-08-26):** `431c2a9`에서 실제 `main.js`의 Drive 목록 요청을 응답하지 않는 Promise로 고정했다. finalizer는 약 3초 경계에서 요청 취소를 정확히 한 번 호출하고, 종료 중 새로 생긴 설정 dirty와 기존 숙제 operation을 모두 recovery marker에 남긴 뒤 WAL checkpoint·DB close와 최종 종료를 완료했다. 실제 서버 반영 후 응답만 유실되는 실계정 조건과 다음 실행 원격 판정은 별도로 남긴다.
- **반복 종료 요청 런타임 검증 완료(2026-08-26):** `62dd7a6`에서 Drive timeout finalizer 시작 100ms 뒤 외부 `app.quit()`을 한 번 더 요청했다. 두 번째 요청은 finalizer를 우회하지 않았고 최초 종료의 3초 drain·취소·marker·WAL/DB 정리가 끝난 뒤 내부 최종 quit에서만 프로세스가 종료됐다.
- **세션 종료 fast path 런타임 검증 완료(2026-08-26):** `35f3dab`에서 실제 main 창에 `query-session-end`를 발생시켰다. 이벤트를 취소하지 않았고 핸들러 반환 시점에 설정 dirty·숙제 operation recovery marker가 이미 디스크에 있었으며 WAL checkpoint도 실행됐다. 실제 Windows 로그오프·시스템 종료 동작은 별도 실기로 남긴다.
- **응답 유실 후 main 재시작 검증 완료(2026-08-26):** `bb2e782`, `4257b5b`에서 지속형 모의 Drive를 연결한 실제 `main.js` 프로세스가 설정 또는 숙제 payload를 원격에 반영한 뒤 각각 응답을 받지 못하게 했다. 첫 프로세스는 3초 종료 제한에서 요청을 취소하고 종료 시점 settings dirty와 checklist outbox 전체를 파일별 recovery marker에 보존했다. 같은 userData와 원격 저장소로 시작한 두 번째 프로세스는 설정의 revision/checksum 또는 숙제 operation을 확인해 이미 반영된 대상 파일을 중복 업로드하지 않고 marker를 제거했으며, 반대 파일의 미전송 변경도 처리해 두 dirty/outbox가 모두 비었다. `013e4ba`에서는 설정/숙제 응답 유실 종료를 각각 10회씩 반복해 20회 모두 operation·recovery marker·1회 요청 취소와 약 3.03~3.06초 종료를 확인했다. 이 과정에서 비동기 debug 로그가 실제 파일에 늦게 기록되어 probe 결과만 `false`가 되는 계측 경합을 발견했고, logger 호출을 동기 관찰하도록 테스트만 안정화했다. `0a9c412`는 1ms polling 전에 끝나는 빠른 종료에서 창 숨김 계측이 `null`이 되는 경합과 같은 비동기 WAL/DB 로그 판독을 보강했다. 일반 종료 10회는 창 숨김 24~28ms·전체 34~42ms, timeout 종료 5회는 창 숨김 8~16ms·전체 약 3.04~3.05초로 모두 통과했다. 제품 결함은 재현되지 않았다. 실제 Google Drive 네트워크 응답 유실은 실계정 검증으로 남긴다.
- **자동 검증 완료(2026-08-25):** 첫 quit만 finalizer를 시작하고 정리 중 두 번째 quit는 대기시키며 finalizer가 허용한 마지막 quit만 통과하는 상태 전이와, drain의 성공·실패·시간 초과 경계를 실행 테스트로 고정했다. 표준 종료는 신규 생산자 정지, 창·트레이 즉시 숨김, config/outbox 저장, 최대 3초 클라우드 drain·취소, WAL checkpoint, DB close 순서를 정적 회귀 검사로 확인했다.
- **자동 검증 완료(2026-08-25):** Windows `query-session-end`에서는 OS 종료를 막지 않고 클라우드 recovery marker, config, 엘소 recovery journal과 WAL checkpoint를 동기식 fast path로 먼저 보존하는 등록·호출 순서를 정적 회귀 검사로 고정했다. 실제 Windows 로그오프·시스템 종료 실기는 별도로 남긴다.
- **일반 종료 실기 부분 통과(2026-08-26):** `93d2922`의 격리 fresh 프로필을 실제 Windows UI `Alt+F4`로 종료했다. 생산자 중지부터 WAL 72/72 checkpoint, DB close와 프로세스 종료까지 로그 시각 기준 9ms로 3초 제한을 통과했고, `65ea23d`에서 세 dirty 조합의 실제 main finalizer도 확인했다. Drive 응답 유실·원격 재확인과 Windows 로그오프·시스템 종료는 별도 실기로 남긴다.
- Phase 종료 게이트: `npm run typecheck`.

### Phase 4 — 대용량 채팅·파일 I/O·모니터 안정화

대상: E-01~E-11, C-05 연동, B-08

진행 상태: 정상 크기 당일 로그의 기존 전체 메모리 검색은 유지하고 32MB 초과 파일만 날짜 헤더 표본과 최근 16MB를 읽는 제한 모드로 분리했다. 제한 모드의 이후 실시간 라인도 최근 메모리 창으로 유지한다. Tail 오류는 watcher를 해제·null 처리한 뒤 1/2/4/8/16초 제한 재연결하며, 지정 로그 경로가 일시 접근 불가해도 다른 자동 탐색 경로로 덮어쓰지 않는다. 주간 워커의 EBUSY/EPERM/EACCES 파일은 최대 4회 재시도하고 끝내 실패한 파일은 정상 파일과 분리해 부분 성공 UI로 보고한다. 채팅·Discord·거래·갤러리 키워드는 소비 직전 trim/빈값 제거/중복 제거/크기 제한을 적용했고, 존재하지 않는 날짜 파일명은 Date 왕복 검증으로 제외한다. Windows Known Folder와 개인·기업 OneDrive 실제 루트를 자동 탐색 후보에 포함하고, BOM/meta charset→앞·중간·끝 표본→EUC-KR fallback 순서로 인코딩을 판별한다. 주간 워커는 256KB 청크를 2,000줄/250 이벤트 이하 유한 배치로 보내고 메인 ACK 뒤에만 진행한다. 시작 시 고정한 snapshot 크기, 파일 fingerprint와 마지막 확정 byte offset 및 파일별 누적치를 로컬 내구 상태에 원자 저장하며, 일반 이벤트는 안정 event ID로 중복 커밋을 막는다. 마정석·엘소·숙제처럼 전체 집계가 필요한 값은 부분 배치가 기존 합계를 낮추지 않도록 파일 누적 상태를 보존하고 완료 마커에서만 총합을 반영한다. 계획된 Phase 4 자동 회귀 범위는 완료했으며 실제 비정상 대형 게임 로그 장시간 실기는 최종 소크 테스트에서 다시 확인한다.

1. 정상적인 수 MB 규모의 당일 채팅은 현재처럼 메모리에 적재해 검색한다. 시작 전 파일 크기를 확인하고 비정상적으로 큰 파일만 제한된 표본+상태 유지형 스트리밍/최근 구간 모드로 전환해 OOM을 방지한다.
2. 당일 검색은 `_todayLines` 메모리 검색을 유지하고 결과 상한만 둔다. 일반 경로에 디스크 오프셋 인덱스를 도입하지 않는다.
3. 주간 워커는 유한 배치로 결과를 전송하고 작업 ID/완료 마커로 재시작과 부분 실패를 구분한다.
4. 파일별 EBUSY/EPERM/EACCES를 제한 횟수 지수 백오프로 재시도한다.
5. Tail 오류는 명시적으로 unwatch/null 처리 후 재연결한다.
6. 실패 파일 목록과 부분 성공을 UI까지 전달한다.
7. 명시 설정된 로그 경로는 일시 장애만으로 자동 덮어쓰지 않는다.
8. Windows Known Folder와 OneDrive 실제 루트를 후보에 추가한다.
9. 채팅/Discord/거래/갤러리 키워드를 소비 직전에 trim/filter/dedupe하고 배열·문자열 크기를 제한한다. 갤러리는 별도 전체 알림 토글이 켜져 있으면 키워드 0개일 때 모든 새 글을 알리고, 거래·지정 단어·외치기는 키워드 0개일 때 알리지 않는 현재 제품 계약을 유지한다.
10. 날짜 파일명은 Date 왕복 검증을 수행한다.
11. 어벤던로드 입장료는 감지 즉시 전체 `profit`/`totalFee`에 반영하고, 가까운 시간의 지역 도전 횟수 이벤트와 timestamp 큐로 결합한다. 만료 시 지역 합계에는 넣지 않고 `unassignedFee`로 보존하며 다음 지역에 넘겨 붙이지 않는다.
12. 인코딩 판별은 BOM/meta charset → 여러 구간 표본 → 게임 로그 기본 인코딩 fallback 순으로 수행해 ASCII-only 표본 오판을 막는다.
13. 주간 동기화 워커는 메인의 ACK를 받은 뒤 다음 유한 배치를 보내는 backpressure를 적용한다.
14. 주간 처리 시작 시 snapshot file size를 고정하고 다음 실행은 마지막 확정 byte offset 이후부터 이어 읽는다.
15. 주간 배치 이벤트에 작업 ID·파일 fingerprint·안정적인 event ID를 부여해 중간 재시작과 재전송을 중복 없이 처리한다.

사이드이펙트 방어:

- CP949/UTF-8 문자 청크 경계, 한글/특수문자/OneDrive 경로, 잠금 실패, 부분 성공 fixture를 추가한다.
- 스트리밍 전환 전후 동일 로그의 검출 결과를 golden fixture로 비교한다.
- 파일 truncate·교체·실시간 증가·잠금 재시도를 조합한 fixture와 worker/main 속도 역전 테스트를 추가한다.
- 당일 로그의 정상/경계/초대형 크기 fixture로 메모리 모드와 안전 스트리밍 모드 전환을 검증하고 검색 결과가 동일한지 비교한다.
- **자동 검증 완료(2026-08-25):** 정상 크기 fixture는 전체 읽기, 경계 초과 fixture는 헤더와 최근 구간만 읽으며 중간 구간을 적재하지 않는 것을 확인했다. 제한 모드 라인 창은 최신 행을 보존하면서 지정 문자 상한 아래로 오래된 행을 제거한다.
- **자동 검증 완료(2026-08-25):** Tail 오류 watcher 해제/null 처리와 1~16초 재연결 경계, 이미 닫힌 watcher 정리 예외 격리, 접근 불가능하지만 값이 있는 사용자 로그 경로의 자동 탐색 금지를 고정했다.
- **자동 검증 완료(2026-08-25):** EBUSY 파일의 두 번 실패 후 성공, ENOENT 즉시 실패, 제한 재시도 간격과 파일별 실패·부분 성공 UI 연결을 확인했다. 공백·중복·과대 키워드 정규화와 200개 상한, 윤년 포함 유효 날짜와 잘못된 월·일 거부도 회귀 검사로 고정했다.
- **자동 검증 완료(2026-08-25):** 리디렉션된 한글 문서 Known Folder, 기업·개인 OneDrive 루트 후보와 환경 변수 확장·중복 제거를 확인했다. 64KB를 넘는 ASCII 헤더 뒤 UTF-8 한글, UTF-8/EUC-KR meta charset과 기존 EUC-KR fixture가 올바른 인코딩으로 판별되는 것도 고정했다.
- **자동 검증 완료(2026-08-26):** UTF-8/EUC-KR 다중 바이트 문자가 256KB read chunk 경계를 가로지르는 fixture에서 검출 결과를 보존했다. 첫 2,000줄 배치의 ACK를 지연하는 동안 다음 배치가 나오지 않는 backpressure, 파일 완료 전 마정석 부분 합계 미반영과 완료 배치의 전체 합계, 같은 fingerprint/offset의 안정 event ID 재생성 및 DB 중복 차단을 확인했다. 원자 상태 파일 왕복, 정상 append 재개, truncate/replace 초기화와 실제 매니저→워커→DB→상태 파일 첫 실행/무변경 재실행도 통과했다.
- **자동 검증 완료(2026-08-26):** 같은 UTF-8/EUC-KR fixture를 기존 전파일 decode·normalize·parse 경로와 새 스트리밍 워커에 각각 통과시켜 마정석 이벤트·총수량, SEED 이벤트·금액과 숙제 누적 결과가 동일함을 golden 비교로 고정했다. 당일 검색은 정상 크기, 전체 읽기 상한과 정확히 같은 경계, 상한을 넘긴 최근 구간 안전 모드에서 보존 대상의 type/timestamp/sender/message/color가 동일했다.
- **격리 런타임 검증 완료(2026-08-26):** 실제 Electron 프로세스에서 Tail 오류를 발생시킨 직후 당일 로그를 일시 이동해 첫 1초 재연결 때 파일이 없는 조건을 만들었다. 2초 후속 예약이 이어지고 파일 복원 뒤 watcher가 재연결됐으며, 기존 marker는 1건 그대로이고 복원 뒤 추가한 live marker만 1건 반영되어 기존 로그 재생·중복 증가가 없음을 확인했다. 40MB 로그에 4MB를 실시간 추가한 별도 검사에서는 최근 16MB 제한 모드, 오래된 4,317줄 제거, 재시작 뒤 제한 모드 유지와 최근 marker 검색을 확인했다. 장시간 실제 게임 로그와 전체 인코딩 조합 실기는 계속 대기한다.
- **실제 Windows 잠금 검증 완료(2026-08-26):** 별도 PowerShell 프로세스가 `FileShare.None`으로 2개 중 1개 로그를 독점 잠근 상태에서 실제 Electron 주간 동기화를 실행했다. 수정 전에는 사전 검사가 1ms 만에 `EBUSY`를 throw했지만, 수정 후에는 100/200/400ms 대기 포함 약 773ms 뒤 정상 파일 1개를 반영하고 잠긴 파일 1개만 `partial`·`failedFiles`로 보고했다. 잠금을 해제한 다음 실행은 두 파일 모두 성공, `partial: false`, 실패 목록 0개, 총 4줄로 수렴해 앞서 성공한 파일의 내구 offset도 보존됨을 확인했다.
- **실제 인코딩 파일 검증 완료(2026-08-26):** UTF-8, UTF-8 BOM, EUC-KR 및 첫 한글 바이트가 256KB read chunk 경계 직전(offset 262,143)에 놓인 UTF-8 로그 4개를 실제 Electron 주간 동기화로 처리했다. 사전 판별은 각각 기대 인코딩과 일치했고 4개 파일·8줄·SEED 4건을 실패 없이 반영했다. 같은 파일 재실행은 검출 4건을 유지하면서 신규 반영 0건으로 끝나 안정 event ID와 내구 offset의 중복 방지도 확인했다.
- **정상 로그·Tail 전체 백오프 검증 완료(2026-08-26):** 8.05MB·50,002줄 당일 로그를 실제 Electron 매니저로 열어 최근 구간 제한 모드가 비활성인 상태에서 파일 앞·뒤 marker를 모두 검색했다. Tail 오류 뒤 파일을 일시 이동해 1/2/4/8/16초 예약을 전부 실행하고 최종 예약 중 파일을 복원했다. watcher 재연결 뒤 기존 marker는 각각 1건, live marker는 1건이었고 메모리 줄 수는 신규 한 줄만 증가해 기존 파일 재생이나 중복 처리가 없었다.
- **실제 프로세스 재시작·사용자 경로 검증 완료(2026-08-26):** 첫 Electron 프로세스에서 1개 파일·2줄·SEED 1건을 반영하고 확정 offset 188을 내구 상태에 저장한 뒤 프로세스를 완전히 종료했다. 같은 userData로 시작한 두 번째 프로세스는 검출 1건을 유지하면서 신규 반영 0건, 동일 offset으로 끝났다. 이어 사용자 지정 로그 폴더를 통째로 일시 이동하고 파일 변경 검사를 실행해도 config의 지정 경로를 다른 자동 탐색 경로로 덮어쓰지 않았고, 폴더 복원 뒤 같은 경로의 watcher가 다시 연결됐다.
- **2분 대형 로그 지속 검증 완료(2026-08-26):** 40.0MB 파일에 120초 동안 초당 120줄, 총 14,400줄을 append해 42.05MB까지 증가시켰다. 최근 창이 16MB 상한에 도달하자 오래된 22,194줄을 제거해 12.77M chars로 내려갔고 최종 live marker를 보존했다. heap은 80.96→44.37MB, RSS는 204.71→130.57MB, 최대 event-loop lag는 14ms였다. 이 과정에서 유효 UTF-8 파일을 손상으로 오인하는 E-11을 재현·수정했고, 동일 42MB 파일은 수정 후 `damaged: false`, 반환 84,150줄의 대체 문자 0개였다. 실제 게임 로그를 이용한 장시간 소크는 별도로 남긴다.
- Phase 종료 게이트: `npm run typecheck`.

### Phase 5 — 창·DPI·클릭 투과·스케줄러·오디오

대상: F-01~F-08, H-01~H-08

진행 상태: F-01~F-06의 디스플레이·창 크기·토스트 수명·프로세스 우선순위 재시도 정책은 구현·자동 검증을 통과했다. F-07·F-08의 자동 포커스/재생성 안전장치도 구현·회귀 검사·전체 자동 테스트를 통과했다. H-01~H-08, 장기 콜백 single-flight, 구조화된 `missed-sleep` 이력과 버프 임계값 복구는 구현·자동 검증을 통과했다. 실제 Windows 100/125/150% DPI·듀얼 모니터·RDP·소형 작업 영역 재현 테스트와 30~60분 무조작 소크 테스트 전이므로 Phase 5 제품 완료 판정은 보류한다.

1. display 리스너를 앱 초기화 시 한 번 등록하고 registry 창과 `overlayWindow`를 모두 복구한다.
2. `display-metrics-changed`의 scaleFactor/bounds 변화는 짧게 디바운스해 현재 게임 rect로 재동기화한다.
3. 관리 창 초기 크기를 현재 작업 영역 폭/높이에 맞추고 최소 크기 정책을 적용한다.
4. 모든 토스트 종료를 공통 제거 함수로 통일해 click-through 상태를 복원한다.
5. 우선순위 상승 실패는 PID별 `1초 → 2초 → 4초 → … → 최대 60초` 지수 백오프를 적용하고 성공 또는 게임 PID 변경 시 초기화한다.
6. 분 타이머와 resume 보정 타이머를 각각 추적하고 세대 토큰으로 하나만 살아 있게 한다.
7. 스케줄 callback은 예외가 나도 `finally`에서 정확히 한 번 재예약한다.
8. 같은 분 재검사를 멱등화하고 날짜 키를 `YYYY-MM-DD`로 바꾼다.
9. 오디오 전환 타이머와 Audio 인스턴스 소유권을 세대 토큰으로 보호한다.
10. throttle 경과시간은 `performance.now()`를 쓰고 Map에 작은 상한/정리를 둔다.
11. buff stop은 정책에 따라 리스너와 `_started`를 정리한다.
12. display 이벤트로 수행한 programmatic move 동안 사용자 위치 저장을 금지해 DPI 전환 반복 시 offset 드리프트를 막는다.
13. 디스플레이 이벤트는 유효 display 집합이 안정된 뒤 처리해 Remote Desktop 연결/해제의 임시 bounds로 창을 이동하지 않는다.
14. 관리 창 크기 정책은 창별로 축소 가능/내부 스크롤/고정 크기를 명시한다.
15. 분 스케줄 callback은 single-flight로 실행하고 이전 실행 중 다음 tick이 오면 중첩 호출하지 않는다.
16. 여러 토스트가 동시에 있을 때 interactive toast 참조 수가 0이 된 경우에만 click-through를 복원한다.
17. 창별 활성화 정책을 도입해 완전 입력 투과형 게임 오버레이만 `noActivate/focusable:false`로 두고, 독은 `showInactive()`로 최초 표시하되 클릭 가능한 정상 창으로 생성한다. 텍스트 입력 도구 창은 정상 focus 창으로 분리하며 독의 클릭·hover·submenu와 접근성 동작을 Windows 실기에서 별도 검증한다.
18. `focusGameWindow()`는 `IsIconic`인 경우에만 `SW_RESTORE`를 호출하고, 그 외에는 show state를 바꾸지 않는다. 정상 복구에서는 top-level 게임을 활성화하는 `BringWindowToTop`을 제거하고 사용자 유발 조건을 통과한 `SetForegroundWindow` 반환값으로만 성공을 판정한다.
19. 자동 복구 경로에서는 가상 Alt 키 fallback을 제거한다. 명시적인 사용자 복귀 명령에서도 외부 앱/메뉴가 전경이면 사용하지 않으며, 불가피하게 유지할 경우 별도 opt-in 진단 플래그 아래 마지막 수단으로만 격리한다.
20. 독 클릭·위치 변경 같은 일시 동작은 사용자 드래그/활성 도구 창이 없을 때만 세대 토큰이 있는 지연 포커스 복구를 수행한다. 예약 시 원인 창/foreground HWND를 기록하고 새 focus·Alt+Tab·창 열기 이벤트에서 취소하며 실행 시 외부 프로세스가 전경이면 폐기한다.
21. 일반 창모드와 창모드 전체화면에서 foreground가 게임/TW-Overlay이거나 외부 창이 반대편 모니터에만 있으면 `TW-Overlay > 테일즈위버 > 게임 모니터 작업표시줄` Topmost 묶음을 유지한다. 외부 앱 bounds가 게임 화면과 겹칠 때만 묶음을 Non-Topmost로 내리고 `외부 앱 > TW-Overlay > 테일즈위버`로 배치한다. `placeGameBelowWindow()`와 포커스 강탈은 복원하지 않으며 최소화·게임 종료·앱 종료에서 게임 Topmost를 반드시 해제한다.
22. 전체화면 bounds 판정은 DPI/DWM 반올림을 고려한 1~2 DIP 허용 오차를 적용하되 display `bounds`와 `workArea`를 구분해 최대화 창을 전체화면으로 오인하지 않는다.
23. foreground HWND, 게임 `IsIconic`, 전체화면 bounds 판정, 포커스 복구 허용/거부 이유, Win32 API 반환값, Z-order 재배치 이유를 진단 로그로 남겨 작업표시줄 노출 경로를 재현 가능하게 한다.
24. 새 포커스 정책은 런타임 설정이 아닌 내부 안전 롤백 플래그로 격리해 Windows 실기 회귀가 발견되면 게임 창 상태를 더 건드리지 않는 보수적 경로로 즉시 되돌릴 수 있게 한다.
25. 창 표시 API를 호출 목적에 따라 분리한다. 게임 상태 동기화·설정 재적용·최소화 복구·창 재생성 같은 백그라운드 경로는 `showInactive()`만 사용하고, 사용자가 메뉴나 단축키로 명시적으로 연 입력 창만 `show()+focus()`를 허용한다. 호출부가 표시 사유(`user-open`, `auto-restore`, `game-resync`, `notification-click` 등)를 반드시 전달하도록 해 암묵적인 활성화를 막는다.
26. 게임 추적 결과가 한 번 `null` 또는 최소화 임계값으로 튀었다는 이유만으로 `hideAll()`과 전체 창 재생성을 수행하지 않는다. 실제 HWND 유효성·`IsIconic`·연속 표본을 짧은 디바운스로 재확인하되, 사용자가 실제로 최소화하거나 게임을 종료한 경우 창이 오래 남지 않도록 최소화/종료 판정을 분리한다.
27. 자동 창 생성·표시·숨김·종료마다 창 키, 표시 사유, 직전/직후 foreground HWND, 게임 추적 상태, `show`/`showInactive` 선택을 기록한다. 데스크톱 알림은 알림 종류와 표시 시점만 같은 타임라인에 기록해 작업표시줄 노출과의 상관관계를 먼저 확인하고, 인과가 확인되기 전에는 알림을 강제로 끄지 않는다.
28. 절전 시작 또는 마지막 scheduler tick과 복귀 시각 사이에 예정됐던 필드보스·커스텀 알람을 계산해 소리/토스트 없이 `missed-sleep` 상태로 알람 이력에 기록한다. 활성 버프 타이머가 절전 중 경고 임계값을 통과한 경우도 재구성 가능한 범위에서 같은 상태로 기록한다. 채팅/지정 단어처럼 절전 중 입력 자체가 없던 이벤트는 생성하지 않는다.
29. 알람 이력 스키마에 `scheduledAt`, `recordedAt`, `deliveryStatus=fired|missed-sleep`을 추가하고 resume+unlock 연속 이벤트에서도 같은 예정 알람이 한 번만 기록되도록 안정 ID로 멱등화한다. 이력 보존 기간을 넘긴 절전 구간은 현재 24시간 정책 밖의 과거 기록을 새로 만들지 않는다.
30. 여러 버프 경고가 같은 tick에 도달하면 현재처럼 모두 표시·기록한다. 하나만 선택하거나 나머지를 누락시키지 않으며, 동일 사운드 파일의 800ms 중복 재생 제한만 유지한다.

- **자동 검증 완료(2026-08-26):** display 리스너가 오버레이 사용 여부와 무관하게 앱 초기화에서 한 번 등록되고 연결·해제·DPI/작업 영역 변경을 모두 수신하도록 고정했다. 이벤트 burst 뒤 같은 display 서명이 250ms 유지될 때까지 복구를 미루며 2초 상한을 적용한다. 100/125/150% scaleFactor 서명 변화, 모니터 배열 순서 불변성, RDP 임시→최종 display 안정화 판정을 실행 테스트로 확인했다. 안정화 뒤 최신 물리 게임 rect로 `syncOverlay`를 호출하고, 게임 좌표가 없을 때 registry 창과 브라우저 오버레이의 임시 복구 이동은 programmatic move로 소비해 저장 오프셋 드리프트를 막는다. 실제 Windows 반복 DPI·모니터 분리·RDP 실기는 남긴다.
- **자동 검증 완료(2026-08-26):** 창 크기 정책을 `fit-work-area`/`user-resizable`/`game-fixed`로 분리했다. 1280×720 작업 영역에서 1420×860 계수 계산기가 여백을 제외한 1240×680으로 열리고, 800×600 작업 영역의 모험일지는 초기 760×560과 같은 유효 최소 크기를 사용해 최소 크기 충돌이 없음을 확인했다. 과소 저장된 포커스 채팅은 360×360 최소값으로 복구하고, 숙제·채팅의 기존 저장 크기 필드와 독의 800×380 게임 고정 크기는 유지한다. 공통 관리 창뿐 아니라 별도 생성 경로인 의상 염색·검 강화 도구도 현재 모니터 작업 영역을 사용한다. 실제 소형 해상도별 콘텐츠 잘림·내부 스크롤 실기는 남긴다.
- **격리 DPI 런타임 부분 통과(2026-08-26):** `8715215` 기반 source Electron을 강제 100/125/150% 배율로 각각 시작해 renderer DPR 1/1.25/1.5와 2560×1392, 2048×1114, 1707×928 DIP 작업영역의 기본 계수 계산기 배치를 확인했다. 이어 강제 2×/3×로 1280×696·854×464 DIP를 만들었고, 854×464에서 계수 계산기의 998px 문서가 816px 창 밖으로 잘리는 F-09를 재현했다. `750ec60` 수정 후 동일 probe는 document client/scroll 폭 811/811px, 세로 1,241px와 실제 scroll 이동으로 모든 영역 접근을 확인했으며 1280×696의 기존 2열 배치도 유지했다. 실제 Windows 배율 설정 전환, 게임 기준 정렬, 모니터 분리·RDP와 1280×720·800×600 물리 작업영역 실기는 별도로 남긴다.
- **자동 검증 완료(2026-08-26):** 사기 탐지 토스트의 직접 DOM 제거 두 경로를 공통 `removeToast`로 교체하고, 모든 interactive 토스트를 중복 추가·제거에 안전한 ID Set으로 추적한다. 보스와 사기 탐지 토스트를 동시에 등록한 fixture에서 참조 수가 `1 → 2 → 1 → 0`으로 변하고, 같은 토스트의 중복 제거는 상태를 바꾸지 않으며 마지막 종료에서만 click-through 복원 callback이 호출되는 것을 렌더러 실행 테스트로 고정했다.
- **자동 검증 완료(2026-08-26):** 게임 프로세스 우선순위 상승 요청을 PID별 single-flight로 만들고 실패 완료 시점부터 1/2/4초 지수 백오프와 60초 상한을 적용했다. 완료 전 중복 호출 차단, 재시도 경계 직전/직후, 성공 후 같은 PID 재호출 금지, 이전 PID의 늦은 응답 무시, 새 PID와 게임 종료 초기화를 실행 테스트로 확인했다. `boostGameProcess()`의 기존 `finally` CloseHandle 계약은 그대로 유지한다.
- **자동 검증 완료(2026-08-26):** resume와 unlock-screen을 연속 발생시켜 보정 타이머가 하나만 남고 놓친 분 이력과 현재 분 콜백이 한 번씩만 실행되는 것을 확인했다. 동기 callback throw 뒤에도 다음 분 실행이 재개되고, 1분을 넘기는 Promise 콜백은 겹치지 않으며 완료 시점의 현재 시각을 기준으로 다음 분 하나만 예약되는 것을 가짜 시계·타이머 실행 테스트로 고정했다. `stop()`은 분/보정 타이머와 두 powerMonitor 리스너를 모두 제거한다.
- **자동 검증 완료(2026-08-26):** 이전 Audio 종료 뒤 예약된 500ms 전환 타이머를 미리보기가 취소하고, 이전 `play()` Promise의 늦은 AbortError가 새 미리보기 Audio를 정지·교체하지 않는 것을 실행 테스트로 확인했다. 미리보기 뒤 대기 사운드는 기존 순차 큐로 재생되고 사용자 음량과 기본 음량 선택도 보존한다. 동일 사운드는 단조 시계 기준 800ms 미만에서만 차단하고 경계 시각에는 허용하며, 서로 다른 사운드 200개를 넣어도 throttle Map이 128개를 넘지 않는 것을 고정했다.
- **자동 검증 완료(2026-08-26):** 같은 13:00 분을 두 번 검사해 골모답·혼란한 대지 analytics가 첫 검사에서는 각각 반환되고 두 번째 검사에서는 0건인 것을 확인했다. analytics 키에 전체 날짜·분·보스명을 포함해 같은 분 복수 보스는 유지한다. `2026-01-15`와 `2026-02-15` 날짜 키가 구분되고 날짜 전환 또는 두 dedupe Set 합계 500개 초과 시 알림·analytics 키를 함께 정리하는 경계를 고정했다.
- **자동 검증 완료(2026-08-26):** buff timer를 중지한 기준 상태에서 start→중복 start→stop→재start→stop 순서로 실행했다. `BUFF_USED` 리스너 수가 `기준 → +1 → +1 → 기준 → +1 → 기준`으로 변해 중복 등록, stop 뒤 잔존, 재시작 불가가 모두 없음을 확인했고 각 stop에서 1초 interval도 정리했다.
- **자동 검증 완료(2026-08-26):** 알람 이력 DB v4에 `scheduled_at`, `recorded_at`, `delivery_status`, `dedupe_key`를 추가하고 기존 행을 기존 timestamp·`fired` 상태로 두 번 실행해도 동일하게 backfill했다. 같은 missed-sleep 안정 키를 두 번 저장해도 한 행만 남고 구조화된 예정/기록 시각과 상태가 보존되며 설정 이력 화면은 `절전 중 놓침`과 두 시각을 구분한다. 필드보스·커스텀 알람은 예정 분 기반 안정 키를 사용한다.
- **자동 검증 완료(2026-08-26):** 120초 버프가 절전 구간 중 60초·10초·5초 임계값을 모두 통과하는 fixture에서 세 이력을 예정 시각 순서로 복원했다. 실제 buff manager의 suspend→resume→unlock 연속 이벤트에서는 5초 임계값이 `missed-sleep` 한 행으로만 저장되고 활성 버프 `warnedAt`에도 반영돼 복귀 직후 소리/토스트로 재생되지 않는다. suspend/resume/unlock 리스너도 stop에서 모두 제거한다.

사이드이펙트 방어:

- 100/125/150% DPI, 보조 모니터 분리, 화면 작업 영역 축소, resume+unlock 연속 이벤트, callback throw, 미리보기 경쟁 테스트를 추가한다.
- DPI 반복 전환 후 위치가 누적 이동하지 않는지, Remote Desktop 임시 display 상태에서 사용자 위치가 보존되는지 검사한다.
- callback이 1분보다 오래 걸리는 fixture로 중첩 실행이 발생하지 않는지 검사한다.
- 듀얼 모니터에서 독 하단→상단 변경, 독 메뉴 클릭, 독 숨김/표시, 같은 모니터·다른 모니터 Alt+Tab, 게임 포커스 복귀를 조합하고 작업표시줄이 게임 복귀 뒤 사라지는지 실기 검사한다.
- 외부 앱이 실제 전경 창인 동안에는 Windows 작업표시줄을 강제로 숨기거나 해당 앱의 포커스를 빼앗지 않는지 검사한다.
- 비활성 독에서 마우스 클릭·hover·submenu가 정상 동작하고, 키보드 입력이 필요한 도구 창만 명시적으로 focus를 얻는지 검사한다.
- 포커스 복구 예약 후 100ms 이내 외부 앱 Alt+Tab, 새 TW 도구 열기, 독 재클릭을 수행해 stale 복구가 포커스를 훔치지 않는지 검사한다.
- 최대화 창과 1~2 DIP 오차의 전체화면 창을 구분하고, 반복 포커스 전환 후 게임 show state·bounds가 변하지 않는지 검사한다. TOPMOST는 게임/TW-Overlay 전경에서만 참이고 외부 앱 전경·최소화·종료 직후에는 반드시 거짓인지 확인한다.
- 독·메뉴·Alt+Tab 조작 없이 30~60분 정상 플레이하는 실기 소크 테스트를 수행한다. 채팅 로그 유입, 보스/거래/갤러리 알림, 설정 자동 반영, 오버레이 활성 상태를 조합하고 작업표시줄 노출 시 직전 창 표시 사유를 로그로 역추적한다.
- 게임 추적 결과에 일시적인 `null`/최소화 표본을 주입한 뒤 창이 불필요하게 닫히거나 포커스를 얻지 않는지 검사하고, 실제 최소화·복원·종료에서는 정해진 시간 안에 정상 정리·복원되는지 검증한다.
- 자동 복구로 재생성되는 브라우저 오버레이·채팅·서브 채팅·숙제 체크·독은 모두 게임 포커스를 유지하고, 동일 창을 사용자가 명시적으로 열었을 때만 입력 가능한 도구 창이 포커스를 얻는지 구분해 검사한다.
- suspend 중 필드보스·커스텀 알람·버프 임계값을 각각 통과시킨 뒤 복귀하여 소리/토스트 없이 이력에만 `missed-sleep`으로 남는지, resume+unlock 중복 이벤트에서 한 번만 기록되는지 검사한다.
- 같은 tick에 여러 버프 임계값을 통과시켜 모든 중앙 경고와 이력이 유지되는지 검사한다.
- 기존 `forward:true`, CloseHandle, 안정 폴링 동작을 고정 회귀 테스트로 보호한다.
- Phase 종료 게이트: `npm run typecheck`.

### Phase 6 — 렌더러 비동기/DOM 상한·릴리즈 게이트

대상: G-02~G-06, 남은 P3 정리, 버전·산출물

진행 상태: 일간/주간 상세의 공유 generation, 월/통계/차트의 월별 generation, 렌더 시점 날짜·원본 row ID를 이용한 단건 삭제를 적용했다. 마정석 묶음은 등급별 합계를 유지하되 수동 원본 자식 행만 삭제 버튼을 제공한다. 채팅 history/search/검색 닫기/탭 전환도 하나의 view generation으로 통합하고 live 이벤트 generation과 분리했다. 채팅 데이터는 메모리에 유지하면서 실제 DOM을 viewport+overscan으로 제한하고, 실측 높이 캐시로 과거 prepend·live append·폭 변경의 앵커를 보존한다. G-02~G-06, 남은 P3와 렌더러 injection 경계, 클라우드 allowlist·개인정보 문서 정합성, 전체 diff·산출물 검토와 최종 자동 게이트를 완료했다. `walkthrough.md`에는 자동 검증 결과와 실기·버전 보류 항목을 기록했으며, 실제 릴리즈 완료 판정은 남은 실기와 사용자 승인 뒤에만 갱신한다.

1. 일간/주간 상세 요청에 공유 generation token을 적용하고 삭제는 렌더된 row id를 사용한다.
2. 월/통계/차트는 캡처한 월과 sequence를 모든 await 뒤 확인한다.
3. 채팅 history/search/닫기/탭 전환은 단일 view generation으로 통합하고 success/catch/finally 모두 stale guard를 적용한다.
4. 채팅 데이터는 메모리에 유지하되 화면에는 viewport 주변 행과 overscan만 실제 DOM으로 만드는 목록 가상화를 적용한다. 고정 2,500개 도달 시 과거/최신 데이터를 임의 삭제하는 정책은 사용하지 않는다.
5. 가상 목록은 실제 측정 높이 또는 안정적인 높이 캐시로 스크롤 위치를 보존하고, 검색 강조·가변 높이·탭 전환·위로 과거 탐색에서도 앵커 행이 튀지 않게 한다.
6. 신규 결함마다 정적 회귀 검사와 렌더러 동작 검사를 추가한다.
7. `package.json`과 lockfile을 v3.0.0으로 맞추고 릴리즈 노트/백업 버전 조건을 일치시킨다.
8. 전체 변경 diff와 생성 산출물 범위를 검토한다.
9. 최종 `npm run typecheck`와 `npm test`를 실행한다.
10. 모든 검증 통과 후 `walkthrough.md`에 변경, 데이터 마이그레이션, 테스트 결과, 잔여 제한을 기록한다.
11. 채팅 화면의 view generation과 live-stream generation을 분리해 새 실시간 이벤트가 정상 history/search 요청을 잘못 무효화하지 않게 한다.
12. 가상화된 채팅 목록은 과거 탐색 종료 후 최신 구간으로 복귀해도 메모리 데이터가 누락되지 않으며, DOM 재사용 시 이전 행의 클래스·검색 강조·이벤트가 새 행에 남지 않게 한다.
13. 일지 grouped timeline은 원본 row ID 목록을 보존하되 ID를 집계 키로 사용하지 않는다. 중복 아이템 합계는 canonical item과 `SUM(amount)`로 유지하고, `source=manual`인 개별 자식 행만 ID 단건 삭제를 허용한다. 자동/legacy-unknown 행과 그룹 전체 삭제 버튼은 제공하지 않는다.
14. stale 요청의 success/catch/finally 모두 같은 generation 검사를 통과할 때만 loading·버튼·결과 상태를 바꾼다.
15. `docs/google-drive-sync.md`에 세 파일별 전체 allowlist, 숙제 병합 데이터, 제외 필드와 이유, 복원 우선순위, 민감정보 정책을 내부 설정 키 단위로 공개한다. 코드의 설정/숙제 allowlist와 문서의 목록이 어긋나면 실패하는 회귀 검사를 추가하고 `PRIVACY_POLICY.md`와 `docs/privacy/index.html`을 같은 기준으로 유지한다.

**자동 검증 완료(2026-08-26):** 요청 generation 자체의 이전 토큰 무효화와 명시적 invalidate를 실행 검사로 확인했다. 일간·주간 상세가 같은 generation을 공유하는지, 월 데이터/요약과 통계/수익 차트가 각 await 뒤 최신 요청만 반영하는지, 삭제 완료 뒤 선택 날짜가 달라졌을 때 이전 화면을 다시 그리지 않는지 정적 회귀 검사로 고정했다. 단일·복수 마정석 묶음에서 `manual` 원본 row ID에만 삭제 경계가 생기는지도 함께 검사했다.

**채팅 요청 자동 검증 완료(2026-08-26):** Club→System history를 역순 완료해 최신 System 결과만 남는지, 진행 중 history 뒤 search가 시작되면 history가 검색 상태를 덮지 않는지 확인했다. 연속 검색에서 이전 요청을 reject해도 최신 검색의 loading 문구와 결과가 유지되고, 검색을 닫은 뒤 늦은 reject가 숨긴 상태를 다시 노출하지 않는지 실행 검사로 고정했다. history 대기 중 live 이벤트를 주입한 뒤에도 정상 history 응답이 반영되며 live handler가 view generation을 변경하지 않는 것을 함께 확인했다.

**채팅 가상화 자동 검증 완료(2026-08-26):** 가변 길이 history 20,000건을 로드한 뒤 실제 `.chat-message-row` DOM이 300개 미만인지 확인했다. 과거 150건 prepend, 최상단↔최신 구간 왕복, 최상단에서 live 1,000건 append를 순서대로 수행해 전체 스크롤 높이가 계속 증가하면서 최과거·최신 데이터가 모두 다시 렌더링되는지 검증했다. prepend·live append와 320px 폭 축소/복원 뒤 앵커 행 위치 오차는 2px 이내였고, 검색 종료 뒤 새 history DOM에는 이전 검색 강조 클래스가 남지 않았다.

**클라우드 allowlist·개인정보 문서 자동 검증 완료(2026-08-26):** 설정·숙제 파일의 `data` 허용 키뿐 아니라 설정/숙제 payload 최상위, 숙제 operation·mutation, 메타 payload와 파일 참조의 전체 필드를 실제 빌더 결과와 문서 표가 정확히 일치하는지 검사한다. 메타는 두 데이터 파일의 `id`·`name`만 복사하는 최소 빌더를 사용하며 추가 필드는 제거한다. `updatedBy`는 Google 이메일이 아닌 installation device ID인지, OAuth·Webhook·로그 경로·절대 사운드 경로·창 좌표·로컬 이력이 두 data allowlist에서 제외됐는지, Markdown/HTML 개인정보처리방침이 같은 포함·제외 기준과 상세 가이드 링크를 제공하는지도 함께 고정했다.

**잔여 P3·renderer injection 완료 감사(2026-08-26):** `isVisible` 없는 레거시 숙제의 보임 해석, DB 초기화 실패 뒤 `getStmt()` 오류 전달·마이그레이션 롤백, 채팅 로그 파일명 날짜 왕복 검사는 각각 실행 fixture가 현재 코드를 직접 통과하는지 재확인했다. 모험일지 로그, 갤러리 감시 키·제목, 커스텀 사운드 option에는 태그·속성 종료 악성 문자열을 실제 Electron DOM에 주입해 텍스트는 보존되면서 새 요소가 생성되지 않고 검증된 숫자 ID만 이벤트에 전달되는지 확인했다.

**전체 diff·산출물 및 최종 자동 게이트 감사(2026-08-26):** `origin/beta/v2.7.0`의 `82a5387`부터 자동 감사 커밋 `d202343`까지는 56개 파일, 6,978줄 추가, 799줄 삭제다. Microsoft Store용 `build/appx` PNG 4개는 병합된 패키징 원본 자산으로 확인했고 `dist`, `dist-tools`, `dist_electron`, `release`, `out` 생성물은 변경 범위에 없다. 이후 격리 실행에서 D-11, E-06, E-10, E-11과 F-09, 교차 숙제 payload의 operation/data 비수렴을 추가 확인·수정해 확정 결함은 80개가 되었으며 Windows 실기 재검증 항목은 실제 증거가 확보된 범위만 개별 승격한다. `npm run typecheck`, `npm test`, `git diff --check`가 통과했고 Electron renderer behavior 검사는 40개다.

**후속 자동 범위 감사(2026-08-26, `3437723`):** 현재 `origin/beta/v2.7.0`의 `93d2922` 이후는 12개 파일, 2,059줄 추가, 50줄 삭제다. 제품 파일은 교차 operation canonical 수렴을 고친 `src/modules/cloudSyncManager.ts`와 854×464 잘림을 고친 `src/coefficient-calculator.html` 두 개이며 나머지는 런타임 probe·회귀 검사·검증 문서다. 생성 산출물 추적과 미완료 TODO/FIXME는 없고, 계산기에서 검색되는 `HACK` 두 건은 게임 능력치 표 제목이다. working tree는 clean이며 원격 push·버전·태그·배포는 수행하지 않았다.

**실기 검증 절차 준비(2026-08-26):** 실제 두 PC 클라우드, 종료·로그오프, DPI·모니터·RDP, Z-order 무조작 소크, 대형 로그·Tail 재연결의 준비 조건·실행 순서·합격 기준·증거 표를 `docs/v3-manual-validation.md`에 분리했다. 설치 파일 SHA-256, 설정 UI의 파일별 checksum/revision/pending, 원격 operation ID와 민감·사용자 값을 제외한 로컬 상태 추출 절차도 추가했다. 모든 결과는 대기로 시작하며 실제 증거가 없는 항목을 완료로 표시하지 않는다.

최종 릴리즈 게이트:

- TypeScript 검사 100% 통과
- 전체 빌드 및 정적 회귀 검사 통과
- 렌더러 동작 검사 전체 통과(현재 기준 40개, 추가 검사는 총수 증가)
- 수만 건 메모리 채팅 → 목록 가상화 스크롤 → 과거 탐색 → 라이브 복귀 시 데이터 누락·스크롤 점프·DOM 무한 증가 없음
- history/search/live 이벤트 순서 역전과 stale reject 검사 통과
- 악성 문자열 XSS fixture 통과
- 신규/구버전/부분 실패 DB 마이그레이션 fixture 통과
- 클라우드 큐·종료 재진입·취소 fixture 통과
- 대용량/잠금/멀티바이트 채팅 fixture 통과
- `git diff --check` 통과
- `walkthrough.md` 자동 검증 기록 작성 완료, 실기·버전 승인 뒤 최종 판정 갱신

## 6. 사용자 결정 기록 (Resolved Decisions)

아래 정책은 사용자 답변과 코드 재검토를 반영한 확정 개발 의도다. 이 범위에는 추가 제품 정책 질문이 남아 있지 않다.

1. **숙제 리셋:** 내장 콘텐츠별 리셋 시각과 커스텀 숙제의 사용자 지정 시각을 보존한다. 전역 오전 6시 통일은 하지 않는다.
2. **동선 시뮬레이터 기본 지도:** 시오칸하임 대장간·골고다의 협곡·공허의 영역 지도 메타데이터를 `INSERT OR IGNORE`로 보장한다. 기존 지도/동선은 덮지 않는다.
3. **종료 UX:** 창과 트레이는 즉시 사라지고 백그라운드 정리는 최대 3초만 수행한다. 미완료 작업은 recovery marker로 다음 시작에 재시도한다.
4. **설정 기본값 병합:** 기존 사용자 설정은 값이 기본값과 같거나 빈 값이어도 보존한다. 앱 업데이트로 새로 생긴 누락 키만 추가하며 배열은 일반 딥 머지하지 않는다.
5. **클라우드 의미:** 일반 설정·숙제·메타를 각각 `tw_overlay_settings.json`, `tw_overlay_checklist.json`, `tw_overlay_sync_meta.json`으로 분리한다. 파일명에는 버전을 붙이지 않고 JSON 내부 `schemaVersion`으로 호환성을 판별한다. 설정 변경은 짧게 debounce해 설정 파일만, 숙제 감지/수동 상태 변경은 로컬 commit+outbox 후 숙제 파일만 실시간 갱신한다. 일반 설정은 클라우드 스냅샷을 사용하고, 숙제 상태는 마지막 동기화본 기준 3방향 병합에서 양쪽이 충돌할 때만 로컬을 우선한다. Discord webhook URL과 PC 종속 경로·위치·설치 정보 및 DB/채팅/알람 이력은 동기화하지 않는다.
   - 회사·집처럼 두 PC의 앱이 항상 실행 중인 경우도 지원한다. 변경한 PC는 즉시 업로드하고 반대편 PC는 게임 실행 중 약 30초, 유휴 중 약 5분의 변경 확인과 앱 시작·로그인·절전 복귀·네트워크 재연결·게임 시작 즉시 pull로 자동 반영한다. 원격 적용은 재업로드하지 않으며, 양쪽 로컬 변경이 겹친 경우에만 operation 기반 3방향 병합으로 수렴시킨다.
6. **새 PC 복원:** 로컬 전용 설치 표식과 앱 데이터 생성 전 `config.json`/`diary.db` 존재 여부로 fresh 프로필을 판정한다. fresh 프로필은 메타로 두 데이터 파일을 찾고 설정과 숙제를 독립적으로 복원하며, 클라우드에 없는 신규 기본 키만 추가한다. 기존 데이터가 있으면 무표식 레거시 설치도 기존 PC로 보며, 일부 파일만 있거나 손상돼 애매한 경우에는 자동 복원하지 않는다.
7. **숙제 pending:** 증가 패턴은 `+N`, 횟수 감지는 절대값 `N`으로 이벤트 순서대로 적용한다. 리셋 경계 이전 pending은 이월하지 않는다.
8. **일지 삭제:** `source=manual`인 개별 row만 ID로 삭제한다. ID는 삭제 식별자일 뿐 집계 키가 아니며 동일 아이템 합계는 canonical item과 `SUM(amount)`로 유지한다. 자동/legacy-unknown 기록과 그룹 전체 삭제는 허용하지 않는다.
9. **숙제 재완료:** 콘텐츠 1회 초기화권 사용 후 다시 완료해도 같은 리셋 주기의 일지에는 최신 완료 한 건만 표시한다. 완료시각·이름·category·type을 함께 UPSERT한다.
10. **DB 종료:** 앱 종료/백업 복원에서 writer 정지, 메모리 flush, WAL checkpoint, DB close를 수행한다. 실패 시 데이터 유실보다 복구 journal을 우선한다.
11. **채팅 파일/화면:** 정상적인 수 MB 당일 파일은 메모리 검색을 유지하고 비정상 대형 파일만 안전 모드로 전환한다. 채팅 데이터는 임의 삭제하지 않고 목록 가상화로 실제 DOM만 viewport 주변에 제한한다.
12. **키워드 0개:** 갤러리는 별도 전체 알림 토글이 켜져 있으면 모든 새 글을 알린다. 거래·지정 단어·외치기는 알리지 않는다. 공백 키워드는 모든 기능에서 제거한다.
13. **어벤던로드 입장료:** 감지 즉시 전체 수익에 반영하고 가까운 지역 도전 횟수 로그에 귀속한다. 시간창 밖이면 미귀속으로 보존하며 다음 지역으로 넘기지 않는다.
14. **대형 도구 창:** 모니터 작업 영역 안으로 줄이고 내부 스크롤을 제공한다.
15. **절전 복귀 알람:** 놓친 알람을 소급 재생하지 않고 `missed-sleep`으로 이력에만 기록한다. 여러 버프 경고가 동시에 발생하면 현재처럼 모두 표시한다.
16. **XP 교환:** 사망 경험치 손실 개념은 제거한다. 경험의 정수는 100억 XP당 1개이며, 교환은 세션 총 획득 XP를 줄이지 않고 교환 후 누적량과 정수 개수만 갱신한다.
17. **우선순위 상승 재시도:** PID별 1·2·4초 지수 백오프를 적용해 최대 60초까지 늘리고 성공/PID 변경 시 초기화한다.
18. **주간 동기화:** 작업 ID·파일 fingerprint·event ID·ACK를 가진 유한 배치와 자동 재개를 사용한다.
19. **창모드 전체화면 작업표시줄:** 외부 앱이 실제 전경인 동안 같은 모니터 작업표시줄 노출은 정상이다. 게임으로 돌아온 뒤 남거나 게임이 계속 전경인데 무조작으로 나타나는 것은 오류다. Shell을 직접 숨기거나 외부 앱 포커스를 빼앗지 않고 foreground 소유권에 따라 게임 묶음의 Topmost를 동적으로 승격·해제하며, 게임 show state는 실제 최소화 외에는 바꾸지 않는다.
20. **Z-order 단일 상태 관리자:** 증상별로 Win32 호출과 예외 조건을 추가하지 않는다. `게임 활성`·`오버레이 활성`·`외부 창-다른 모니터`·`외부 창-게임 모니터`·`비활성` 상태를 한 관리자가 판정하고 실제 z-order 쓰기도 전담한다. 포커스·전경 창 이동·안정 폴링·TW-Overlay 창 생명주기는 현재 HWND만 전달한다. 동일 상태이고 실제 창 순서도 정상이면 쓰지 않으며, 게임과 입력 큐를 결합하는 cross-process owner/parent 방식은 사용하지 않는다.

## 7. 수정 계획 역검토 결과 및 필수 방어 설계

현재 계획의 방향은 유효하지만 아래 방어 없이 단순 구현하면 새로운 릴리즈 차단 결함이 발생할 수 있다.

| 영역 | 잠재적 신규 결함/사이드이펙트 | 필수 방어 |
|---|---|---|
| 설정 저장 | Windows rename 실패, 임시 파일 잔존, 원본 유실 | 동일 폴더 임시 파일·flush·재시도·성공 전 원본 보존·시작 시 유효본 선택 |
| 설정 스키마 | 신규 기본값 병합이 기존 사용자의 false/0/빈 배열까지 덮음, 미인식 위험 키 실행 | 키 존재 여부 기반 missing-only 병합, 배열 비병합, 필드별 복구와 quarantine 분리 |
| SQLite 마이그레이션 | 기본 지도/금액 복구 중 기존 지도 메타데이터·동선 또는 중복 행 정보 손실 | `INSERT OR IGNORE`, 원문 기반 복구·canonical 병합·반복 실행 멱등성 |
| 복구 journal | DB commit 후 응답 유실 시 다음 시작에서 이중 적용 | 연산 ID·commit marker·중복 실행 안전성 |
| 클라우드 데이터 분류 | 일반 설정과 숙제 상태에 같은 파일·덮어쓰기 규칙을 적용해 사용자 설정 또는 실제 플레이 상태 손실 | 설정/숙제/메타 파일 분리, 일반 설정은 클라우드 필드 교체, 숙제 상태는 3방향 병합+양쪽 충돌만 로컬 우선+tombstone, PC 종속·민감 필드 제외 |
| 분리 파일 부분 성공 | 설정 업로드만 성공하고 숙제/메타가 실패하거나 반대로 성공해 전체 동기화 상태를 오표시 | 파일별 revision·dirty·오류 상태, 독립 재시도, 새 PC 부분 복원 표시. 메타는 파일 ID 생성/교체 때만 마지막에 게시 |
| 실시간 업로드 폭주 | 슬라이더·텍스트 입력과 채팅 로그 묶음이 요청을 과도하게 생성하거나 종료 flush를 밀어냄 | 설정 1~2초 debounce, 숙제 최대 500ms coalesce, 전역 single-flight 전송 큐와 파일별 dirty drain |
| 숙제 동시 수정 | 두 PC의 read→PATCH가 교차해 완료 해제·횟수 감소가 사라지거나 오래된 로컬이 최신 클라우드를 막음 | base/local/remote 3방향 병합, 안정 operation ID·로컬 outbox·재다운로드 검증·누락 operation 재시도 |
| 동기화 echo | 클라우드 설정 적용이 로컬 저장 이벤트를 일으켜 같은 파일을 계속 재업로드 | 적용 origin token과 bootstrap upload barrier, 원격과 다른 결과만 후속 업로드 |
| 일반 설정 클라우드 우선 | 오프라인 로컬 변경이 다른 PC에서 갱신된 클라우드 설정에 의해 되돌아감 | 클라우드 revision 명시, 적용 직전 원자적 로컬 백업, 변경 요약·되돌리기 제공. 동기화 중 로컬 변경은 새 revision으로 업로드 |
| 새 PC 판정 | 기존 버전 사용자가 설치 표식이 없거나 일부 파일이 손상됐다는 이유로 fresh로 오인되어 로컬이 덮임 | 앱 데이터 생성 전 config/DB 존재 여부와 로컬 전용 설치 표식을 함께 검사, 레거시 데이터가 하나라도 있거나 판정이 애매하면 established/needs-confirmation 처리 |
| Drive abort | 클라이언트는 실패로 보지만 서버 업로드는 완료 | checksum/modifiedTime reconciliation |
| 작업 큐 | 현재 작업이 자신이 enqueue한 작업을 기다리는 데드락 | 외부 drain loop와 dirty flag |
| 채팅 배치 | 부분 커밋 뒤 재실행 중복, worker→main 큐 메모리 증가 | event ID 멱등성·ACK backpressure·재개 지점 |
| 당일 채팅 메모리 | 정상 파일까지 복잡한 인덱스로 바꾸거나 초대형 파일을 그대로 읽어 성능/메모리 회귀 | 수 MB 메모리 경로 유지, 파일 크기 안전 상한 초과 시에만 스트리밍 안전 모드 |
| DPI 복구 | 프로그램 이동을 사용자 이동으로 저장해 반복 배율 변경 시 좌표 드리프트 | programmatic move 구간 저장 금지 |
| 전체화면 작업표시줄—명시적 조작 | 독 클릭 후 Shell 작업표시줄이 남거나 `focusable:false`로 click IPC가 끊기고, 시작 메뉴 Topmost HWND 뒤에 삽입하면서 승격·강등을 반복해 깜박임 | 독 최초 표시만 `showInactive`, 실제 클릭 활성화, foreground 동안만 동적 Topmost, 외부 Topmost 전경은 `HWND_TOP` 기준 Non-Topmost 배치, 최소화·종료 즉시 해제, 최종 스타일 재검증 |
| 전체화면 작업표시줄—무조작 발생 | 순간적인 최소화 오판 뒤 자동 창이 포커스를 얻거나 작업표시줄이 활성 묶음 위로 재승격 | 최소화/종료 연속 표본 확인, 자동 경로 `showInactive`, 안정 폴링의 작업표시줄 상대 순서 읽기 검사와 조건부 복구, 30~60분 소크 테스트 |
| 듀얼 모니터 외부 창 이동 | 반대편 모니터에서 포커스를 유지한 채 게임 모니터로 창을 끌면 foreground 이벤트가 없어 안정 폴링 전까지 Topmost 게임 뒤로 가려짐 | 전경 창의 `EVENT_OBJECT_LOCATIONCHANGE`를 별도 감시하고 실제 게임 영역 진입·이탈 시 즉시 같은 z-order 정책 재평가, 종료 시 훅 해제 |
| 독 단축키 표시 지연 | 숨길 때 `close()`하여 매 표시마다 renderer·메뉴·아이콘을 다시 생성하고 100ms 이상 지연 | 독 모드 감지 시 renderer를 숨은 상태로 사전 로딩하고 `hide/showInactive`로 재사용, 숨긴 상태에서 위치 확정 후 표시. 일반 사이드바 모드 전환·앱 종료 때만 실제 close |
| 독 renderer 재사용 후 설정 갱신 | 재생성이 사라지면서 사이드바에만 전달하던 퀵링크 변경이 기존 독 UI에 반영되지 않음 | 퀵링크 저장 후 `broadcastConfig()`로 숨겨진 독을 포함한 관련 renderer에 전송하고 독은 `onConfigData`에서 위치 클래스·메뉴·퀵링크 전체를 다시 렌더링 |
| 분 스케줄러 | callback 장기 실행과 다음 tick 중첩 | single-flight 및 중복 tick skip |
| 채팅 목록 가상화 | DOM 재사용 시 잘못된 행 내용/클래스가 남거나 가변 높이에서 스크롤 점프 | 안정 key, overscan, 높이 측정 캐시, 재사용 상태 초기화, 앵커 보정 |
| 일지 삭제 | row ID를 집계 키로 사용해 같은 아이템 합계가 분리되거나 그룹 전체가 삭제됨 | ID는 단건 삭제 전용, canonical item+SUM 집계 유지, `source=manual` 자식 행만 삭제 |
| 절전 누락 알람 | resume+unlock 중복 기록 또는 복귀 순간 소급 재생 | 예정 시각 기반 안정 ID, `missed-sleep` 이력 전용 기록, 소리/토스트 금지, 24시간 보존 경계 |

### 공통 엣지케이스 테스트 원칙

- 모든 마이그레이션은 동일 입력에 두 번 실행해도 결과가 같아야 한다.
- 모든 recovery/동기화 연산은 commit 직전·직후·응답 유실·프로세스 종료 지점에서 재실행해 중복이 없어야 한다.
- 모든 비동기 렌더 요청은 응답 성공뿐 아니라 stale reject와 stale finally를 포함해 검증한다.
- 파일 I/O는 EBUSY/EPERM/EACCES, 파일 증가, truncate, 교체, 한글·특수문자 경로를 조합해 검증한다.
- 시간 로직은 시계 역행, 장치 간 시각 차이, 자정/월 변경, 월요일 리셋 직전·직후, resume+unlock 연속 이벤트를 포함한다.
- 디스플레이 로직은 100/125/150%, 보조 모니터 제거, Remote Desktop 임시 display, 반복 DPI 전환 후 좌표 불변성을 검증한다.
- 창모드 전체화면 포커스 로직은 독 위치 변경·메뉴 클릭·창 닫기·동일/다른 모니터 Alt+Tab뿐 아니라 아무 조작 없는 장시간 플레이와 자동 창 재생성도 구분하고, 게임 재활성화 뒤 작업표시줄이 자동 복구되는지 Windows 실기에서 검증한다.
- 포커스 복구는 Win32 API 성공 반환만 믿지 않고 실제 `GetForegroundWindow()`를 재확인하며, 실패 시 반복적인 포커스 강탈·가상 키 입력·Shell 작업표시줄 조작 없이 종료한다.
- 목록 가상화와 generation 로직은 검색·탭 전환·과거 탐색·실시간 유입을 임의 순서로 섞고 실제 DOM 수가 viewport+overscan 범위에서 안정적인지 검증한다.

## 8. 승인 조건

사용자가 다음을 명시적으로 승인한 뒤 Phase 1 코드 수정을 시작한다.

1. 이 문서의 Phase 1~6 순서와 범위
2. 사용자 결정 기록 1~19의 확정 정책
3. 현재 작업 트리의 기존 변경 위에 안전하게 누적 패치하는 방식

승인 전에는 코드, 테스트, 버전, 릴리즈 노트, `walkthrough.md`를 수정하지 않는다.
