<?xml version="1.0" encoding="utf-8"?>
<!--
  Desktop Bridge용 Visual C++ 런타임 의존성과 Store 자동 실행 StartupTask를 선언한다.
  Koffi의 Windows 네이티브 모듈은 MSVCP140/VCRUNTIME140/VCRUNTIME140_1을 사용하므로
  이 선언을 제거하면 Visual C++ 런타임이 없는 Store 인증 PC에서 main process가 시작 즉시 종료된다.
-->
<Package
   xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
   xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
   xmlns:desktop="http://schemas.microsoft.com/appx/manifest/desktop/windows10"
   xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities">
  <Identity Name="${identityName}"
    ProcessorArchitecture="${arch}"
    Publisher='${publisher}'
    Version="${version}" />
  <Properties>
    <DisplayName>${displayName}</DisplayName>
    <PublisherDisplayName>${publisherDisplayName}</PublisherDisplayName>
    <Description>${description}</Description>
    <Logo>${logo}</Logo>
  </Properties>
  <Resources>
    ${resourceLanguages}
  </Resources>
  <Dependencies>
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="${minVersion}" MaxVersionTested="${maxVersionTested}" />
    <PackageDependency
      Name="Microsoft.VCLibs.140.00.UWPDesktop"
      MinVersion="14.0.24217.0"
      Publisher="CN=Microsoft Corporation, O=Microsoft Corporation, L=Redmond, S=Washington, C=US" />
  </Dependencies>
  ${capabilities}
  <Applications>
    <Application Id="${applicationId}" Executable="${executable}" EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements
       BackgroundColor="${backgroundColor}"
       DisplayName="${displayName}"
       Square150x150Logo="${square150x150Logo}"
       Square44x44Logo="${square44x44Logo}"
       Description="${description}">
        ${lockScreen}
        ${defaultTile}
        ${splashScreen}
      </uap:VisualElements>
      <Extensions>
        <desktop:Extension Category="windows.startupTask"
          Executable="app\resources\app.asar.unpacked\dist\store-update-helper\TWOverlay.StoreUpdateHelper.exe"
          EntryPoint="Windows.FullTrustApplication">
          <desktop:StartupTask TaskId="TWOverlayStartup" Enabled="false" DisplayName="TW-Overlay" />
        </desktop:Extension>
      </Extensions>
    </Application>
  </Applications>
</Package>
