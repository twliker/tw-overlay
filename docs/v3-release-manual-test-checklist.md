# TW-Overlay v3.0.0 사용자 실기 테스트 체크리스트

이 문서는 실제 PC에서 테스트하면서 직접 체크하고 코멘트를 남기는 실행용 문서다. 기술적인 합격 기준과 증거 수집 명령은 [`v3-manual-validation.md`](v3-manual-validation.md)를 참고한다.

## 작성 규칙

- 실제로 확인해 정상인 항목만 `[x]`로 변경한다.
- 실패하거나 애매하면 `[ ]`를 유지하고 코멘트에 증상을 적는다.
- 실패 시 가능하면 `시각`, `PC-A/PC-B`, `직전 동작`, `예상 결과`, `실제 결과`, `debug.log 관련 구간`을 남긴다.
- 이메일, OAuth token, Discord Webhook, 캐릭터 이름, 로컬 절대경로는 문서나 공유 로그에 기록하지 않는다.
- 결함이 발견되면 이후 데이터가 오염될 수 있는 항목은 중단하고 먼저 수정·재검증한다.

## 0. 이번 테스트 정보

- 테스트 시작일:
- 테스트 종료일:
- 검증 커밋:
- 설치 파일 이름:
- 설치 파일 SHA-256:
- PC-A 역할/환경:
- PC-B 역할/환경:
- 테일즈위버 모드: 창모드 / 창모드 전체화면
- 모니터 구성 및 배율:
- 공통 메모:

## 1. 테스트 준비와 기본 회귀

- [ ] **PRE-01 — 두 PC가 동일한 검증 빌드를 사용한다.**
  - 확인 내용: 설치 파일 SHA-256 또는 검증 커밋이 일치한다.
  - 코멘트:
  - 증거/로그:

- [ ] **PRE-02 — 기존 사용자 데이터를 백업했다.**
  - 확인 내용: 두 PC의 앱 백업을 별도 위치에 보관했다.
  - 코멘트:
  - 증거/로그:

- [ ] **PRE-03 — 2.7.1 사용자 데이터가 개발 빌드에서 유지된다.**
  - 절차: 2.7.1에서 설정·캐릭터·숙제 상태를 만든 뒤 최신 개발 빌드를 실행한다.
  - 합격: 기존 설정, 캐릭터와 숙제 완료·횟수 상태가 기본값으로 덮이지 않는다.
  - 코멘트:
  - 증거/로그:

- [ ] **PRE-04 — 앱의 기본 사용자 흐름이 정상이다.**
  - 확인 내용: 앱 시작, 트레이, 사이드바/독, 설정 저장, 숙제 수동 체크, 일지 열기와 앱 재시작이 정상이다.
  - 코멘트:
  - 증거/로그:

## 2. 관리자 권한 가짜 테일즈위버 검사

관리자 권한으로 실행한 Codex 또는 PowerShell에서 다음 명령을 실행한다.

```powershell
npm run test:zorder:windows
```

- [ ] **FIX-01 — 관리자 권한 실제 HWND 검사가 통과한다.**
  - 합격: 출력의 `passed`와 `elevated`가 모두 `true`이고 종료 코드가 0이다.
  - 코멘트:
  - 증거/로그:

- [ ] **FIX-02 — 창모드 시나리오가 통과한다.**
  - 합격: tracker 탐지, 게임 foreground 보존, 외부 창 우선, 오버레이 순서 복구가 모두 `true`다.
  - 코멘트:
  - 증거/로그:

- [ ] **FIX-03 — 창모드 전체화면 시나리오가 통과한다.**
  - 합격: borderless bounds와 게임 위치·크기·Topmost 불변, 외부 창 우선, 오버레이 순서 복구가 모두 정상이다.
  - 코멘트:
  - 증거/로그:

상세 사용법: [`fake-talesweaver-fixture.md`](fake-talesweaver-fixture.md)

## 3. Google Drive와 두 PC 순차 동기화

두 PC에서 같은 Google 계정으로 로그인한다. 기존 개발용 원격 데이터가 섞일 가능성이 있으면 새로운 테스트 계정을 사용한다. 원격 AppData를 임의로 삭제하거나 수정하지 않는다.

- [ ] **CLD-01 — 최초 로그인과 초기 동기화가 정상이다.**
  - 절차: PC-A에서 로그인하고 설정·숙제·메타 세 파일의 전송 완료를 확인한 뒤 PC-B에서 로그인한다.
  - 합격: 체크섬 오류 없이 두 PC의 profile이 `established`가 되고 같은 설정·숙제 상태가 표시된다.
  - 코멘트:
  - 증거/로그:

- [ ] **CLD-02 — PC-A의 숙제 변경이 PC-B에 도착한다.**
  - 절차: PC-A에서 미완료 숙제를 플레이하거나 수동 변경한 뒤 PC-B에서 확인한다.
  - 합격: 게임 실행 중 약 30초 이내에 반영되거나 앱 시작·게임 시작 시 즉시 반영된다.
  - 코멘트:
  - 증거/로그:

- [ ] **CLD-03 — PC-B의 숙제 변경이 PC-A에 도착한다.**
  - 절차: PC-B에서 다른 미완료 숙제를 변경한 뒤 PC-A에서 확인한다.
  - 합격: PC-A의 기존 변경을 잃지 않고 PC-B의 변경도 함께 반영된다.
  - 코멘트:
  - 증거/로그:

- [ ] **CLD-04 — 양쪽 앱을 켜 둔 상태에서 다음 변경도 자동 수렴한다.**
  - 절차: 한쪽 앱을 종료하지 않고 다른 PC에서 숙제를 추가 진행한 뒤 돌아와 확인한다.
  - 합격: 앱 재시작이나 수동 동기화 없이 변경이 도착한다. 게임 미실행 상태에서는 최대 약 5분까지 기다린다.
  - 코멘트:
  - 증거/로그:

- [ ] **CLD-05 — 원격 pending 숙제는 캐릭터 선택 팝업으로 표시된다.**
  - 사전 조건: 숙제에 캐릭터가 두 명 이상 등록돼 있다.
  - 합격: 수신 PC에서 팝업이 뜨고 사용자가 선택한 캐릭터 한 명에게만 적용된다. `나중에 하기` 후에는 다음 표시 시 다시 나타난다.
  - 코멘트:
  - 증거/로그:

- [ ] **CLD-06 — 설정 변경이 별도로 동기화된다.**
  - 절차: PC-A에서 안전한 일반 설정 하나를 변경하고 PC-B에서 확인한 뒤 반대로 반복한다.
  - 합격: 숙제 상태를 바꾸지 않고 약 1.5초 debounce 후 설정 파일만 업로드되며 상대 PC의 다음 pull에 반영된다.
  - 코멘트:
  - 증거/로그:

- [ ] **CLD-07 — PC 전용·민감 정보는 동기화되지 않는다.**
  - 확인 내용: Discord Webhook, OAuth token, 로그 경로, 로컬 파일 경로, 커스텀 사운드가 다른 PC 값으로 덮이지 않는다.
  - 코멘트:
  - 증거/로그:

- [ ] **CLD-08 — 네트워크 복구 뒤 자동으로 재수렴한다.**
  - 절차: 한 PC를 오프라인으로 전환해 숙제를 변경하고 다시 온라인으로 전환한다.
  - 합격: 온라인 복구 감지 뒤 변경을 업로드하고 상대 PC에도 도착한다.
  - 코멘트:
  - 증거/로그:

- [ ] **CLD-09 — 재시작 뒤 동기화 상태가 유지된다.**
  - 절차: 변경 수렴 후 두 앱을 각각 종료·재시작한다.
  - 합격: 숙제·설정이 되돌아가지 않고 outbox/recovery가 정리되며 불필요한 재업로드가 없다.
  - 코멘트:
  - 증거/로그:

- [ ] **CLD-10 — 수렴 후 echo upload가 없다.**
  - 절차: 양쪽에서 아무 조작 없이 2분 이상 관찰한다.
  - 합격: 설정·숙제 revision이 이유 없이 계속 증가하지 않는다.
  - 코멘트:
  - 증거/로그:

> 두 PC에서 1초 이내에 변경하는 물리적 동시 충돌과 같은 숙제의 완료/해제/횟수 충돌은 필수 실기에서 제외했다. 이 정책은 교차 overwrite, 응답 유실과 재시작을 포함한 자동 테스트로 고정돼 있다.

## 4. 새 PC와 부분 복원 UI

이 절은 기존 데이터 백업을 확인한 뒤 수행한다. 로컬 파일을 직접 조작해야 하는 항목은 별도 테스트 프로필에서만 진행한다.

- [ ] **RST-01 — 완전히 새 프로필을 `fresh`로 판정한다.**
  - 합격: 원격 설정과 숙제를 독립적으로 찾고 복원 대상과 상태를 표시한다.
  - 코멘트:
  - 증거/로그:

- [ ] **RST-02 — 기존 사용자 프로필을 `established`로 판정한다.**
  - 합격: 기존 `config.json` 또는 일지 DB가 있으면 신규 PC로 오인해 기본값이나 원격값을 자동 덮어쓰지 않는다.
  - 코멘트:
  - 증거/로그:

- [ ] **RST-03 — 애매한 프로필을 `needs-confirmation`으로 판정한다.**
  - 합격: 일부 파일만 있거나 손상된 경우 사용자 선택 전 자동 업로드·복원을 하지 않는다.
  - 코멘트:
  - 증거/로그:

- [ ] **RST-04 — 설정과 숙제를 파일별로 선택해 복원할 수 있다.**
  - 합격: 설정만 또는 숙제만 선택할 수 있고 선택하지 않은 종류의 로컬 값은 유지된다.
  - 코멘트:
  - 증거/로그:

- [ ] **RST-05 — 한 파일의 실패가 정상 파일 복원을 막지 않는다.**
  - 합격: 손상·generation 불일치 파일은 오류로 표시되고 다른 정상 파일은 적용된다.
  - 코멘트:
  - 증거/로그:

- [ ] **RST-06 — 복원 전 백업으로 되돌릴 수 있다.**
  - 합격: 되돌리기 후 기존 로컬 설정·숙제가 복구되고 이후 클라우드도 그 상태로 정상 수렴한다.
  - 코멘트:
  - 증거/로그:

## 5. 종료·응답 유실·Windows 세션 복구

- [ ] **END-01 — 일반 종료 시 UI가 즉시 사라진다.**
  - 합격: 창과 트레이가 먼저 사라지고 사용자가 종료 지연을 느끼지 않는다.
  - 코멘트:
  - 증거/로그:

- [ ] **END-02 — 오프라인 변경을 가진 상태로 종료해도 데이터가 남는다.**
  - 절차: 오프라인에서 설정과 숙제를 각각 변경하고 앱을 종료한다.
  - 합격: 다음 실행에서 변경이 유지되고 온라인 복구 뒤 업로드된다.
  - 코멘트:
  - 증거/로그:

- [ ] **END-03 — 업로드 중 종료·응답 유실이 복구된다.**
  - 절차: 업로드 직후 네트워크를 끊고 종료한 뒤 네트워크를 복구해 다시 실행한다.
  - 합격: 원격 반영 여부를 revision/checksum/operation으로 판단해 중복 없이 완료하거나 재시도한다.
  - 코멘트:
  - 증거/로그:

- [ ] **END-04 — Windows 로그오프 뒤 데이터가 보존된다.**
  - 합격: 로그오프가 취소되거나 오래 지연되지 않고 재로그인 뒤 config, 숙제와 일지가 유지된다.
  - 코멘트:
  - 증거/로그:

- [ ] **END-05 — Windows 시스템 종료·재부팅 뒤 데이터가 보존된다.**
  - 합격: 종료가 취소되지 않고 재부팅 뒤 숙제 outbox/recovery와 일지 DB/WAL이 정상 복구된다.
  - 코멘트:
  - 증거/로그:

## 6. 실제 게임 Z-order·작업표시줄·포커스

창모드와 창모드 전체화면에서 각각 수행한다.

- [ ] **ZOR-01 — 게임 활성 상태에서 모든 Twoverlay 창이 게임 위에 있다.**
  - 확인 내용: 독, 체크리스트, 브라우저 오버레이, HUD와 입력 도구 창의 기존 내부 순서도 자연스럽다.
  - 코멘트:
  - 증거/로그:

- [ ] **ZOR-02 — 브라우저·카카오톡을 누를 때 게임이 같이 튀어나오지 않는다.**
  - 절차: 게임을 최소화하지 않고 뒤에 둔 상태에서 같은 모니터의 외부 앱을 반복 클릭한다.
  - 합격: 외부 앱만 전경에 남고 테일즈위버와 Twoverlay가 함께 재등장하지 않는다.
  - 코멘트:
  - 증거/로그:

- [ ] **ZOR-03 — 다른 모니터의 외부 앱도 자연스럽게 사용할 수 있다.**
  - 합격: 외부 앱의 기존 Z-order를 바꾸지 않고 게임과 Twoverlay가 불필요하게 앞으로 나오지 않는다.
  - 코멘트:
  - 증거/로그:

- [ ] **ZOR-04 — 게임으로 돌아오면 Twoverlay만 게임 위로 복귀한다.**
  - 합격: 게임에 포커스가 남고 외부 앱이나 게임 HWND를 강제로 이동한 흔적이 없다.
  - 코멘트:
  - 증거/로그:

- [ ] **ZOR-05 — 게임 최소화·복원 동작이 자연스럽다.**
  - 합격: 최소화 시 파생 창이 남지 않고 복원 시 포커스를 빼앗거나 전체 창을 반복 재생성하지 않는다.
  - 코멘트:
  - 증거/로그:

- [ ] **ZOR-06 — 독 조작과 작업표시줄이 정상이다.**
  - 절차: 독 클릭, 메뉴 열기, 숨김/표시, 상단↔하단 변경과 퀵링크 변경을 반복한다.
  - 합격: 독이 클릭 가능하고 작업표시줄이 게임 위에 고착되지 않으며 변경이 즉시 반영된다.
  - 코멘트:
  - 증거/로그:

- [ ] **ZOR-07 — 외부 앱 사용 중 자동 HUD와 알림이 앞으로 나오지 않는다.**
  - 확인 내용: 불꽃·젤리삐 효과, HUD 편집, 사냥 동선, 업데이트 공지, 채팅·보스·거래·갤러리 알림.
  - 코멘트:
  - 증거/로그:

- [ ] **ZOR-08 — 자동 생성 창이 게임 포커스를 빼앗지 않는다.**
  - 확인 내용: 브라우저, 채팅, 숙제와 독 창이 자동 생성·재표시될 때 게임 입력이 유지된다.
  - 코멘트:
  - 증거/로그:

- [ ] **ZOR-09 — 입력 창은 사용자가 클릭했을 때 정상 동작한다.**
  - 합격: 설정·계산기·검색·텍스트 입력 도구가 클릭 후 입력 가능하고 닫은 뒤 게임 복귀가 자연스럽다.
  - 코멘트:
  - 증거/로그:

- [ ] **ZOR-10 — 30~60분 무조작 소크를 통과한다.**
  - 사전 조건: 채팅·보스·거래·갤러리 알림과 설정 자동 반영을 켠다.
  - 합격: 작업표시줄 노출, 오버레이 후퇴, 독 클릭 불가, 포커스 강탈과 반복 깜박임이 한 번도 없다.
  - 코멘트:
  - 증거/로그:

## 7. DPI·모니터·RDP·창 크기

- [ ] **DSP-01 — Windows 배율 100%, 125%, 150%에서 정렬이 정상이다.**
  - 합격: 각 배율에서 앱을 다시 시작해도 게임 기준 오버레이 위치와 사용자 offset이 유지된다.
  - 코멘트:
  - 증거/로그:

- [ ] **DSP-02 — 보조 모니터 연결·분리와 주 모니터 변경이 안전하다.**
  - 합격: 창이 사라지거나 엉뚱한 화면에 고착되지 않고 저장 위치가 불필요하게 덮이지 않는다.
  - 코멘트:
  - 증거/로그:

- [ ] **DSP-03 — 배율을 반복 전환해도 위치가 누적 이동하지 않는다.**
  - 코멘트:
  - 증거/로그:

- [ ] **DSP-04 — 작은 작업 영역에서도 도구 창을 사용할 수 있다.**
  - 확인 내용: 1280×720과 가능하면 800×600급 작업 영역에서 계수 계산기·모험일지 등 대형 창이 화면 안에 있고 내부 스크롤이 된다.
  - 코멘트:
  - 증거/로그:

- [ ] **DSP-05 — RDP 연결·해제 뒤 위치가 복구된다.**
  - 환경상 RDP가 불가능하면 `미수행 — 환경 없음`으로 코멘트를 남기고 체크하지 않는다.
  - 코멘트:
  - 증거/로그:

## 8. 실제 플레이 장시간 회귀

- [ ] **PLY-01 — 숙제 자동 감지와 캐릭터 적용이 정상이다.**
  - 합격: 감지 내용이 올바른 숙제·캐릭터에 적용되고 중복 완료·횟수 증가가 없다.
  - 코멘트:
  - 증거/로그:

- [ ] **PLY-02 — 일지·아이템·금액 기록이 정상이다.**
  - 합격: 신규 기록, 재완료 최신 시각, 금액 단위와 수량이 실제 플레이와 일치한다.
  - 코멘트:
  - 증거/로그:

- [ ] **PLY-03 — 채팅·알림·커스텀 사운드가 정상이다.**
  - 합격: 필요한 이벤트만 한 번 표시·재생되고 앱 재시작 뒤 중복 리스너나 반복 알림이 없다.
  - 코멘트:
  - 증거/로그:

- [ ] **PLY-04 — 실제 게임 로그를 장시간 처리해도 안정적이다.**
  - 합격: 플레이 중 앱 멈춤, 지속적인 메모리 증가, 로그 누락·중복과 Tail 재연결 실패가 없다.
  - 테스트 시간:
  - 코멘트:
  - 증거/로그:

- [ ] **PLY-05 — 앱과 게임을 반복 시작·종료해도 상태가 유지된다.**
  - 합격: 오버레이 표시 설정, 숙제, 일지, 동기화 상태와 사용자 창 위치가 되돌아가지 않는다.
  - 코멘트:
  - 증거/로그:

## 9. Microsoft Store 패키지

- [ ] **MST-01 — 제출할 AppX 내부 검증을 통과한다.**
  - 실행: `npm run verify:appx -- dist_electron/twOverlay-3.0.0.appx`
  - 합격: `passed: true`이며 TW-Overlay 타일 4개, `runFullTrust`·`allowElevation`, `Microsoft.VCLibs.140.00.UWPDesktop`, 앱 진입점과 Windows x64 네이티브 모듈이 모두 확인된다.
  - AppX SHA-256:
  - 코멘트:

- [ ] **MST-02 — 서명된 Store 패키지의 타일이 제품을 올바르게 나타낸다.**
  - 사전 조건: Partner Center 서명본·비공개 flight 또는 제출본 복사본의 테스트 서명 패키지를 사용한다.
  - 합격: 설치 화면과 시작 메뉴의 정사각형·와이드 타일 어디에도 Electron 기본 이미지가 없고 TW-Overlay 전용 이미지가 표시된다.
  - Windows 빌드:
  - 코멘트:
  - 증거:

- [ ] **MST-03 — Store 설치본을 처음 실행하고 다시 실행해도 시작 크래시가 없다.**
  - 합격: 두 실행 모두 UAC 승인 뒤 사이드바가 열리고 `koffi.node` 또는 `A JavaScript error occurred in the main process` 대화상자가 나타나지 않는다. 설치된 package dependency에 `Microsoft.VCLibs.140.00.UWPDesktop` x64가 존재한다.
  - 첫 실행 결과:
  - 두 번째 실행 결과:
  - 코멘트:
  - 증거/로그:

- [ ] **MST-04 — Store 런타임 기본 기능과 업데이트 분기가 정상이다.**
  - 합격: 주요 오버레이 하나와 숙제 체크리스트를 열고 정상 종료할 수 있으며 GitHub 자체 업데이트 다운로드가 시작되지 않는다.
  - 코멘트:
  - 증거/로그:

## 10. 선택적 고위험 복원 경계

자동 테스트로 이미 검증한 항목이다. 실제 Google Drive AppData나 정상 사용자 데이터를 직접 손상시켜야 한다면 수행하지 않아도 된다. 전용 테스트 계정과 백업이 있을 때만 진행한다.

- [ ] **OPT-01 — 설정 파일만 존재하는 부분 복원**
  - 코멘트:

- [ ] **OPT-02 — 숙제 파일만 존재하는 부분 복원**
  - 코멘트:

- [ ] **OPT-03 — 손상된 메타와 정상 데이터 파일 분리 처리**
  - 코멘트:

- [ ] **OPT-04 — 중복 파일명에서 유효한 후보 선택**
  - 코멘트:

- [ ] **OPT-05 — generation 불일치 파일만 격리**
  - 코멘트:

## 11. 최종 판정

- [ ] 필수 항목의 모든 실패가 수정되고 같은 시나리오를 재검증했다.
- [ ] 미수행 항목의 사유와 릴리즈 영향이 코멘트에 기록돼 있다.
- [ ] 두 PC의 설정·숙제 상태가 최종적으로 일치한다.
- [ ] 수렴 후 2분 동안 echo upload가 없다.
- [ ] 실게임 Z-order 30~60분 소크가 통과했다.
- [ ] Windows 종료·재부팅 뒤 데이터가 보존됐다.
- [ ] Microsoft Store AppX 내부 검증과 서명 설치본 2회 실행이 통과했다.
- [ ] 민감 정보가 문서·화면 캡처·공유 로그에 포함되지 않았다.
- [ ] 최종 `npm run typecheck`, `npm test`, `git diff --check`가 통과했다.

### 최종 코멘트

- 전체 결과: 통과 / 조건부 통과 / 실패
- 릴리즈 차단 결함:
- 후속 개선 사항:
- 최종 확인자:
- 확인 시각:
